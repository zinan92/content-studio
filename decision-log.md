# Decision log — content-studio

> Keep only durable decisions. A functional PR records its decision and
> Gotchas; a pure deploy/status change is exempt unless it changes a durable
> operating fact.

## 2026-09-13 — content-studio 地基与 M1 边界

- **Context:** Park 批准新建 `zinan92/content-studio`，需求说明要求先复用已有仓库并在 M1 先保存创作者后台数据。
- **Decision:** `content-downloader` 是唯一下载入口但必须先修抖音 `_sanitize_cookies` 导入错误；`content-extractor` 是唯一转写入口；`content-intelligence` 与 `content-workbench` 只作参考，不作为依赖或扩建基座。
- **Why:** 2026-09-13 实测结果显示下载器和转写器已有可复用能力，另外两仓库的归因逻辑或产品目标不同；保持单一下载链路可减少数据和风控边界分叉。
- **Alternatives rejected:** `douyin-downloader`、`douyin-downloader-1`、`MediaCrawler` 不在范围内；不在 `content-workbench` 上扩建。
- **Evidence:** [`docs/spec.md`](docs/spec.md) 第 3 节“复用边界”；样稿 [`docs/prototype/index.html`](docs/prototype/index.html)。
- **Gotchas:** cookies 仅存仓库外本机；抖音抓取低频串行；出现验证码/风控立即停止；content-extractor 只收视频、不收纯音频，且拆解台不依赖其 LLM 摘要。

## 2026-09-13 — 对标库与报告规则

- **Context:** 需要固定 M1–M3 的对标对象和报告表达边界，避免把样稿假设写成产品规则。
- **Decision:** 初始对标账号为 dontbesilent 聊赚钱、千雪AI、柱子哥TzFilm；蜗牛学长移除；前端“粘贴主页链接加入对标账号”是必做；标签与“四步结构检查”只作为待验证参考维度。
- **Why:** 需求说明已批准名单、移除项和前端入口；公开数据无法验证对标账号涨粉，因此标签不应控制爆款判定。
- **Alternatives rejected:** 不把后台加入账号作为唯一入口；不把“5 类生态位”或四步评分作为自动判定门槛。
- **Evidence:** [`docs/spec.md`](docs/spec.md) 第 2、4、5、6 节；线上样稿 [Claude artifact](https://claude.ai/code/artifact/6d9672af-68eb-45cc-b65e-98fd07bea1dd)。
- **Gotchas:** 爆款倍数必须除以账号自己的点赞中位数，默认门槛 5×；M1 闸门是 Park 读完三份样本报告并确认“说中了”，此前不开始 M2。

## 2026-09-13 — M1 报告证据边界

- **Context:** content-extractor 的可选 LLM 重构/摘要因本机 OAuth 不可用而降级，但 M1 要求仍能以转写文字完成结构拆解。
- **Decision:** M1 报告使用 content-extractor 真实生成的带时间戳转写作为主材料；结构标签、跑题标记和“为什么爆/为什么散”由本地确定性规则生成，并在每条结论中保留原文时间点；LLM 摘要不是流水线依赖。
- **Why:** 真实转写已成功（千雪AI 69 段、柱子哥 198 段、Park 898 段），而可选 LLM 服务返回 auth unavailable；把两者分开使报告仍可复验且不把未验证推断写成事实。
- **Alternatives rejected:** 不用样稿里的静态报告数组冒充真实结果；不因摘要服务不可用而伪造“完成”；不把标签或四步结构检查作为爆款判定规则。
- **Evidence:** M1-3 PR [#4](https://github.com/zinan92/content-studio/pull/4)；本机报告回执 `~/.config/content-studio/m1/pipeline-run.json`；[需求说明](docs/spec.md) 第 3、4、5 节。
- **Gotchas:** 报告只在本机保存；每次结论必须能追到转写时间点；M1 三份报告必须由 Park 阅读确认后才允许进入 M2。

## 2026-09-14 — M1-3 返工：结构判断交给模型，数字由代码算

- **Context:** Park 验收 M1-3 时发现报告"没说中"。核查代码：主线和"为什么爆"按标题关键词返回写死的句子；跑题靠一张取自样稿示例文字的关键词表；分段是固定 45 秒窗口；`_metric_value` 实际对所有指标返回空，报告没有引用任何数字。结果是只对三条验收样本看起来成立，Park 视频里的主论点（不要搭 harness）反被标跑题。
- **Decision:** 取代上一条"M1 报告证据边界"中"本地确定性规则生成结构"的做法。改为：通读全部带时间戳转写后由模型给出主线、按意思分段、逐段判断是否服务主线并写理由、写"为什么爆 / 为什么散"；所有数字（收藏/赞、转发/赞、评论/赞、账号点赞中位数倍数、后台 2 秒跳出/平均观看等）由代码计算后喂给模型，结论必须引用数字，否则带校验错误重试一次、仍不合格则该条失败；证据引文由代码按时间点回查真实转写，不采用模型复述的原文；Park 自己的视频额外分析"观众平均看到的前 N 秒"。模型调用可注入，默认走本机已登录的 `claude -p`（可用 `CONTENT_STUDIO_LLM_CMD` 替换）。转写纠错用 `config/glossary.json` 词表。来源链接只保留 `douyin.com/video/<id>`。
- **Why:** 结构判断需要理解语义，关键词规则必然对着样本过拟合；而数字和引文如果交给模型会被编造，所以判断与计算分离。
- **Alternatives rejected:** 继续扩充关键词表（无法泛化）；让模型自己读数和引用原文（不可复验）；走 CLI Proxy（本机代理密钥与配置文件不一致，调用被拒）。
- **Evidence:** PR [#4](https://github.com/zinan92/content-studio/pull/4) 返工提交；测试 `tests/test_structure.py` 含"源码不得出现样本专属字符串"的守卫；泛化样本 dontbesilent `7651653378111540495`。
- **Gotchas:** 不得为了让某条样本"看起来对"而在代码里加任何标题、关键词或写死结论——守卫测试会拦；报告质量问题改提示词或校验规则，不改成特例。对标账号没有后台数据，"前 N 秒"一节必须缺省而不是编造。

## 2026-09-14 — M2/M3 在 Park 夜间授权下连续交付

- **Context:** Park 睡前要求"做到 production ready，明早看到完整产品，做完自己验收"。原计划 M1 闸门需 Park 确认"说中了"才进 M2。
- **Decision:** 视为 Park 对连续推进 M2、M3 的明确授权，但 M1 的"说中了"仍需 Park 本人确认，验收报告单列四份样本报告请他判断。M2 用本地 SQLite（账号 / 作品 / 任务 / 设置）；抖音账号资料用 `/aweme/v1/web/user/profile/other/`、作品列表复用 content-downloader 签名客户端；爆款 = 点赞 ÷ 该账号非置顶作品点赞中位数；自动入队按"当前等待中的自动任务数"封顶（默认 8），已有报告的作品不入队。M3 用 FastAPI + 原生 JS，直接沿用样稿的样式与四页结构，只监听 127.0.0.1，默认端口 8780（8765 被本机其他服务占用）。
- **Why:** 单用户本机工具，SQLite + 无构建前端最少依赖、重启即恢复；按"等待中"封顶可以防止多账号各自触发导致一次入队几十条、通宵占满模型调用。
- **Alternatives rejected:** 自动加载 launchd 定时任务（违反零无人值守管理自动化，且 Park 不在场）；在 content-workbench 上扩建（已批准只作参考）；前端框架 + 构建链（对单用户本机工具是负担）。
- **Evidence:** PR [#7](https://github.com/zinan92/content-studio/pull/7)（M2）、M3 PR；本机真实数据：4 个账号、160 条作品、17 条 ≥5× 爆款、队列自动产出报告。
- **Gotchas:** 置顶作品不计入中位数但仍可能是爆款；图文作品不算爆款；对标视频播放量抖音不公开（显示为空，不是 0）；`write-schedule` 只写 plist，启用命令要 Park 本人执行；页面轮询时报告页不重绘，避免打断阅读。

## 2026-09-14 — "为什么散"允许纯结构原因

- **Context:** 真实队列里 1/8 的任务因"为什么散"连续三次没有引用数字而失败（例如"三条路径之间缺少清晰过渡"这类结构问题本身没有对应指标）。
- **Decision:** "为什么爆"至少一条必须引用数据；"为什么散"能用数据就引用，纯结构原因可不带数字（报告仍会附上代码计算的跑题秒数）。失败提示改为中文并提示可重试。
- **Why:** 强制给结构问题配数字会逼模型硬凑数据或反复失败，两者都比没有数字更糟。
- **Alternatives rejected:** 继续加重试次数（浪费调用且不解决问题）；由代码替模型补一条数字结论（把代码结论冒充模型判断）。
- **Evidence:** 本 PR 测试 `test_scatter_reasons_may_be_structural_without_numbers`；失败任务重试。
- **Gotchas:** 不要再把"每条结论必须带数字"加回来。

---


## 2026-09-14 — 升级为内容生产工作台（Epic #16）

**Decision:** Park 批准把内容拆解台升级为自己的内容生产工作台（方案 https://claude.ai/code/artifact/bb2c26c5-4d36-4acd-acba-3bc9d56e5686）：网页形态；左侧主题（今天/收集/选题/加工/发出/复盘/工具），首页固定「今天」；「我的账号」可以有多个；别人写的 skill 只链接署名不复制；仓库公开。

**Why:** Park 的内容链路（Obsidian 剪藏与原始输出 → 卡兹克写作 → 研习室；口播 → 剪辑 → 抖音）零件都在，缺每天打开就知道做什么的主线。检验标准：是否让 Park 每天多发一篇、多拍一条。

**Gotchas:** 前端按模块拆文件（`today.js` 等先于 `app.js` 加载，向 `window.VIEWS` / `window.TODAY_CARDS` 注册，运行时才使用 `app.js` 的全局 helper）；今日卡片按 `data-card` 复用节点，轮询重绘不会闪。

## 2026-09-14 — 热点不接抖音站内搜索（#20）

**Decision:** 热点只用三类不需额外抓取的来源：对标账号 48 小时爆款、Park 自己的 AI/财经日报头条、剪藏与日报里重复出现的话题（本地统计）。

**Why:** 用已签名客户端对 `/aweme/v1/web/search/item/` 发 1 次请求即返回 `antispam_check / hit_shark`。North Star 要求遇风控立即停止、不绕过；浏览器自动化抓搜索同样属于绕过。

**Gotchas:** 同一条新闻会在日报的「快讯」「深读」「视频更新」重复出现，头条和话题计数都按标题前 16 个字去重；英文词过滤月份缩写等噪声。

## 2026-09-14 — 文章线：卡兹克写作出草稿，研习室手动导入（#22）

**Decision:** 选题「写文章」在后台调用本机 `claude -p --model opus`，允许 Skill/Read/Glob/Grep，提示词要求使用 khazix-writer 写法、作者是 Park；草稿存 `~/.config/content-studio/drafts/topic-<id>/`，不写回 Obsidian。「交给研习室」= 复制正文 + 打开设置里的研习室电脑后台地址 + 状态改待发；发布仍由 Park 在研习室完成。

**Why:** 研习室的 device intake 接口（Obsidian 插件用的那条）真实环境验收仍未通过（wechat-xingqiu#207），现在接自动入草稿箱会建立在未验证的合同上。

**Gotchas:** khazix-writer 默认会带卡兹克本人的署名、邮箱和固定结尾，提示词明确禁止，`extract_article` 再做一道检查（命中即重试）。launchd 下调用 claude CLI 需要环境里有 USER/LOGNAME。真实测试：一篇 2700 字的文章约 2 分钟。

## 2026-09-14 — 口播 workflow 以「读产物」方式嵌入（#40）

**Decision:** 工作台不运行 ask-park-video，只读项目目录：按 workflow v2.6 每一步的完成证据判断当前 Step 与审批门（H1/H2/H3），project.json 的状态标签只在该步没有必需产物时才算数；给出一键复制的继续命令。唯一的写操作是「新建项目文件夹」（README + 拍摄提纲副本）。

**Why:** 口播 workflow 是长任务、要改媒体文件、有三个人工审批门，由 Claude/Codex 在前台跑更安全；工作台负责让 Park 随时知道剪到哪、下一步做什么。后台代跑列为 P3-2，需 Park 确认。

**Gotchas:** 现有项目在外接硬盘 `/Volumes/Phone SSD/视频/exports`，都是 v2.6 之前的目录（无 project.json，成片叫 final-video.mp4 或 delivery/final-video.mp4），按旧版只识别是否有成片；硬盘没接时页面提示而不报错。worktable.html 以 `CSP: sandbox allow-scripts allow-downloads` 返回，能用但碰不到工作台 API。

## 2026-09-14 — worktable 导入由浏览器读文件（#40 后续）

**Decision:** H1 阶段在工作台里「选择导出的 worktable.json」或粘贴 JSON 导入到 `analysis/worktable.json`，校验 schema `park-video-worktable/v1` 与项目名，不默认覆盖。`analysis/worktable.html` 不加沙箱返回。

**Why:** worktable 把 Park 的选择存在 localStorage，沙箱源里不可用，刷新会丢；该页面由 Park 自己的 skill 生成，可信。

**Gotchas:** launchd 下的 Python 读 `~/Downloads` 会触发 macOS 隐私授权弹窗并阻塞请求（实测卡死），所以服务端永远不扫描「下载」文件夹。

## 2026-09-14 — 后台代跑口播 workflow，审批在工作台里点（#56）

**Decision:** Park 授权后，工作台可以在后台用 `claude -p --model opus`（允许 Skill/Bash/Read/Write/Edit/Glob/Grep，工作目录与 --add-dir 都是项目目录）跑 ask-park-video 到下一个审批门；一次只跑一个，进程组独立于服务（重启不打断），退出码写在日志旁边，可中止。H1/H2/H3 在工作台里看材料后点批准，写入 project.json 的 approvals（by Park / via content-studio）并追加 process-log。项目停在审批门时不允许启动。

**Why:** Park 在外面也要能推进剪辑；三个审批门仍然由 Park 本人决定。

**Gotchas:** H1 批准要求 worktable.json 已导入；旧版目录（无 project.json）不能代跑。

## 2026-09-14 — 一键发布必须 Park 确认；堵住跨站写请求（#57）

**Decision:** 发布分两段：准备（冻结成片路径、平台文案、草稿/公开方式）→ Park 在工作台点「确认发布」才执行，确认窗口 30 分钟，服务端没有确认不执行。通道复用 content-ops 的视频号 / B 站 / YouTube 脚本（视频号 headless），登录只看凭据文件日期，不代登录。所有非 GET 的 `/api` 请求要求 `X-Content-Studio: 1` 且 Origin 同源；后台代跑额外禁用 WebFetch/WebSearch。

**Why:** Park 授权「替我做决策，每次发布需要我确认」。安全复查指出：工作台经密码代理暴露在外网，浏览器缓存的 Basic Auth 会让其他网站能用简单表单 POST 触发代跑或确认发布。

**Gotchas:** 2026-09 时视频号、B 站 cookie 与 YouTube token 都是 5 月的，第一次发布大概率需要 Park 在电脑上重新登录。

## 2026-09-15 — 简化版：每天只看读 / 拍 / 发（#62）

**Decision:** 首页主线从 8 步收成 3 组（读、拍、发），其余步骤作为小字挂在组下，判定规则不变；首页加「连续拍摄天数」，断了标红。左侧主导航只留 今天 / 每日统筹 / 选题 / 视频 / 文章 / 每周复盘，其余放进「更多」。热点页取消，对标爆款和日报头条放到统筹页下方，并作为统筹的输入。Skills 并入设置页。文案只自动生成抖音、视频号、研习室，其余平台收在「更多平台」手填，已有记录保留。每周复盘只留数据表、做对 / 问题和「下周只改一件事」。对标爆款自动拆解只拆 ≥5×（独立设置 auto_enqueue_threshold），每天最多 2 条；对标报告 7 天没看，读列表时自动归档（不开后台定时任务）。

**Why:** 第一性原理复盘：瓶颈是拍摄，不是信息。Park 说「确实有点复杂，按你的建议先改一个版本」。

**Gotchas:** `/api/hot`、`hot.py`、七个平台的 PLATFORMS 和发布记录都保留，只是界面收起；旧书签 #hot / #skills 分别落到统筹和设置。

## 2026-09-15 — 开头 15 秒检查读项目字幕，不另做转写（#64）

**Decision:** 视频页的剪辑进度里加「开头 15 秒」：读口播项目里已有的字幕（成片 final-video.srt 优先，其次 Hook 剪辑，再次原始录音 source.srt），取前 60 秒，和提纲「主线」一起交给 sonnet 判断主张第一次出现的秒数；≤15 秒算过。秒数必须对上某句字幕开头、引用必须是字幕原文，否则重试。只抛话题（「今天聊聊……区别」）不算说出主线。结果存 drafts/topic-N/opening.json，不改项目文件。

**Why:** 复盘数据：Park 的观众平均只看 14–26 秒，2 秒跳出 35–39%，跑题越少表现越好。录完就能知道开头有没有问题，比发出去 48 小时后再拆解早。

**Gotchas:** 第一版提示词把「抛出话题」判成说出主线（9/7《AI用法变化》误判为 1.4 秒通过），已改提示词，同一条复测为「前 60 秒没说出主线」。

## 2026-09-15 — 工作台按「进项 → 加工中 → 已发出」重组（#66）

**Decision:** 不再按时间（今天 / 每日统筹）组织，按内容所处阶段组织：进项（Newsletter、Clippings、我收藏的、我写的）、加工中（一条条视频，提纲 → 录制 → 剪辑 → 待发，列由文件证据自动决定）、已发出（数据与复盘）。文章和视频不再分开入口，新选题一律 formats=both；`formats` 列保留不删。每天 08:30 后日报一出，服务端自动生成「今天推荐拍」，每天只跑一次，失败不自动重试。进项里已经做成视频的笔记标「已在做 / 已发出」；Park 在工作台外拍过的笔记可以标「拍过了」，推荐不再使用它。「剪藏 / 收藏 / 原始输出」改名为 Clippings / 我收藏的 / 我写的（内部 key 不变）。推荐去重读近 90 天全部已发视频标题。

**Why:** Park 2026-09-15：「前、中、后，即 Input、Processing 和 Output」「文章和视频其实是同样的」「自动出今天推荐拍」；并指出推荐里的「问卷」其实前两天已经拍过（9/11 FDE 那条）。

**Gotchas:** 标题相似度认不出「问卷」和「给每一个业务匹配一个 FDE」是同一条，所以用手动「拍过了」而不是自动猜。

## 2026-09-15 — 三段界面上线：进项 / 加工中 / 已发出（#68）

**Decision:** 左侧只剩三段（深色导轨上连成一条线，进项靛蓝、加工中钨丝橙、已发出信号绿，页面强调色跟着阶段走）加设置。删掉 今天 / 每日统筹 / 选题 / 文章 / 热点 页面和对应代码（today.js、topics.js、briefing.js、hot.js、recent.js、review.js、collect.js，`/api/today/plan`、`/api/hot`、日报头条与高频话题统计）。文章变成一条视频的页签；复盘、我的视频、对标雷达、拆解报告收在「已发出」的子导航，拆解队列折叠在报告页底部。已发出概览用图表：每条视频倍数（90 天时间轴）、2 秒跳出 / 平均观看 / 涨粉 / 收藏赞走势。只有挂上抖音视频才算「已发出」，文章先发不让卡片离开看板。

**Why:** Park：「前、中、后，即 Input、Processing 和 Output」「页面太普通」「复盘可以放 dashboard、charts」。

**Gotchas:** 真实数据截图不进公开仓库（README 旧截图已删，没有换成真实数据截图）。「问卷」那条笔记正是看板上《越会用 AI 的人，越容易做出没人要的东西》的来源，Park 说已经以 9/11 FDE 那条拍过，已归档该选题并把笔记标「拍过了」。

## 2026-09-15 — 一条视频只留提纲、剪辑、发布、研习室文章

- **Context:** Park 走了一遍前端：「写拍摄提纲」点不动；提纲太细；「文案在每个平台都写一遍…没有必要」，而且和提纲、文章重复。进项页布局也不好看。
- **Decision:** 提纲改为一句主线 + 4–8 条要点；删掉按平台 AI 生成文案（`POST /api/topics/{id}/copy` 与 `CONTENT_STUDIO_COPY_CMD`），「文案与平台」页签并入「发布」，只写一个标题 + 一段简介，保存到抖音 / 视频号 / B 站 / YouTube 各 key，各平台只提示字数；「文章」改名「研习室文章」作为可选页签。进项页：Newsletter 收成一行，来源页签带未处理数，宽屏自动打开第一条未处理的，阅读器去掉内联样式。
- **Why:** Park 拍视频只看要点；标题和简介各平台基本一样，平台差异只是字数上限，不值得三次模型调用和三份要维护的草稿。
- **Alternatives rejected:** 保留按平台生成但默认折叠（仍然是三份重复草稿）；删掉研习室文章（它是付费产品的长文，不是提纲的重复）。
- **Evidence:** 浏览器实测：点「写拍摄提纲」发出 `POST /api/topics/10/outline`；发布页保存时同一标题写入 4 个平台，视频号 19/16 标红；375px 宽无横向滚动。
- **Gotchas:** 上次重构误删了 `[data-outline]` 的点击监听，现在在页签渲染后直接绑定；一键发布仍读 `copy.json` 里对应平台的 key，所以前端必须保存到每个平台 key；抖音没有一键发布通道，仍需手动发；剪藏网页里的内联 `style` 会让视频盖住阅读器标题，DOMPurify 现在禁止 `style`。

## 2026-09-15 — 三点评分：痛点具象度、认知反差度、交付可行性

- **Context:** Park 定下判断一条内容值不值得拍的三点，要一个「内容 QA」角色；参考 dbskill 和 Anna 的知识库，并问能否把 Anna 直接部署到后台。
- **Decision:** 新增 `qa.py`：提纲写完自动评分（sonnet），每点 1–5 分，每分必须引用提纲或素材原话，引不出原话最多 2 分，素材里没有结果就判「素材太薄」；结论为 可以拍 / 先补再拍 / 素材太薄，显示在提纲页和看板卡片上。今天推荐拍的每条带三点分，首选必须总分最高。提纲 prompt 要求三点各落在具体一条上。完整标准写成 Park 本机的 skill `~/.claude/skills/park-content-qa/SKILL.md`，工作台运行时读取（可用 `CONTENT_STUDIO_QA_GUIDE` 改路径），找不到时用仓库里的简版标准。
- **Why:** 三点是闸门而不是写作器：在 Park 花一次拍摄之前拦下痛点空、没反差、没结果的题；引用原话校验防止模型给出好听但没根据的分数。
- **Alternatives rejected:** 把 Anna 整个部署进工作台——她的职责是定位、产品和变现链路判断，且明确不做具体内容生产，放进提纲环节会重新变复杂；把 dbskill 的 SKILL.md 复制进仓库——它是 CC BY-NC 4.0，本仓库公开，只借思路、自写文字并在 skill 里致谢。
- **Evidence:** 真实跑「为什么这么多人，到今天还看不到 AI 的影响力？」：痛点 2、反差 4、交付 1，判「素材太薄」，建议补一个 Park 亲历的小案例。
- **Gotchas:** skill 文件含 Park 的定位和目标人群，只放本机不进仓库；旧的每日推荐没有三点分，卡片上不显示；评分失败不影响提纲，只在提纲页显示原因和「重评」。

## 2026-09-16 — 工作台只读 AI 日报，不读财经日报和晨报

- **Context:** 进项页原来显示三份日报：AI 日报、财经日报、晨报。Park 问要不要留财经。
- **Decision:** 只保留 AI 日报。财经日报、K 线日报、晨报留在 Obsidian 供交易线使用，工作台不读、也不进「今天推荐拍」的材料。
- **Why:** 18 条已发视频全是 AI 认知类，没有一条来自行情日报；行情材料改变的是当天操作，不是明天的内容。喂给推荐只会稀释材料、增加噪音。
- **Alternatives rejected:** 保留但默认折叠（仍然进推荐 prompt）；把所有 newsletter 目录合并改名（会动到三条每天出东西的流水线，收益只是好看）。
- **Evidence:** `/api/mine` 18 条视频标题与倍数；最好的几条是「AI 时代的明牌机会」21.7×、「how to be successful」10.6×，都是认知类；最接近金融的「AI 时代盯盘的人 100% 亏钱」1.3×。
- **Gotchas:** 晨报的 HTML 不再可读，`/api/vault/raw` 对 `009_morning brief` 返回 404；如果以后要拍财经内容，先拍两条 AI × 金融验证数据，再考虑把财经日报接回来。

## 2026-09-16 — Obsidian 的 Clippings 目录改名为 002_clippings

- **Context:** Park 统一 vault 编号，把 Web Clipper 的保存目录改成 `002_clippings` 并重命名了文件夹。
- **Decision:** `INBOX_SOURCES` 的剪藏目录改为 `002_clippings`（显示名仍是 Clippings）；服务启动时跑一次 `rename_note_prefix("Clippings", "002_clippings")`，把 `inbox_triage` 和 `topics.note_paths` 里的旧路径改过来。
- **Why:** 选题和「拿来做 / 拍过了」的记录都按笔记路径存；只改目录不改库，会让已有选题的素材读不到、进项页重新显示成没处理过。
- **Alternatives rejected:** 让 Park 手动重连素材；同时支持新旧两个目录（vault 里已经没有旧目录，多一条路径只会长期留坑）。
- **Evidence:** 迁移前库里有 3 条 triage、4 个选题引用 `Clippings/...`；重启后进项页 Clippings 条目正常，选题 9、10 的素材仍可读。
- **Gotchas:** 改名函数是幂等的，跑第二次返回 0；drafts 里的 outline.meta.json 仍存着旧路径字符串，只用于显示来源，不影响读取。

## 2026-09-16 — Anna 常驻工作台右栏，所有对话都 route 给她

- **Context:** Park 想在工作台里跟一个「有血有肉的员工」对话，而不是自己点功能。先后讨论过三段各配一个助手、MCP 工具箱、DeepSeek Harness；最终决定内容线只有 Anna 一个角色，所有事归她。
- **Decision:** 新增 `anna.py` 与右栏面板。角色 = Obsidian 里的 `001_role/content_editor Anna.md` + 其 knowledge + 本机 park-content-qa skill，每轮重新读，改 Markdown 即改人。每轮把 Park 正在看的页面整理成 `<工作台>` 块发给她（进项 / 看板 / 某条视频 / 已发出各不同），对话按页面分线程、按 `scope` 存库，用 Claude Code 的 session 续聊。她只动嘴：回答末尾的 `[动作]` 行由页面变成按钮，Park 点了才调用现有 API。模型默认 Claude Code Sonnet，命令可配置。
- **Why:** 用 skill（Markdown）而不是 MCP：Park 的原话「Skill 就是文字，改 Markdown 就能改人」；工作台已有全部数据，缺的只是对话层。Claude Code 而不是 Codex：本机已登录、其余功能同源、可续聊、一轮约 10–15 秒。
- **Alternatives rejected:** 三段三个助手（角色没被验证有独立需求）；MCP 工具箱（多一层打包，收益要等换框架时才有）；DeepSeek Harness（alpha、无安全审计、主模型要 API 付费、自带能跑命令的工具）；让 Anna 直接调用工具（工作台外网可达，聊天框会变成 shell）。
- **Evidence:** 真实问「这个提纲第一句够不够狠」，14 秒回答，引用了提纲原句和三点评分 2/5，给出具体改法并附「重写提纲」按钮。
- **Gotchas:** `--system-prompt` 每轮都传（约 35KB），resume 失败时自动开新会话；同一页面同时只跑一轮；面板宽 380px，1180px 以下 320px，900px 以下变成右侧浮层；角色文件不在仓库里，找不到时用一段最小人设兜底。

## 2026-09-16 — Codex 走查：判断不一致的两处真 bug，一处提示

- **Context:** Park 让 Codex 走查工作台，提了 6 点。逐条对照代码和真实数据核实，不是全盘照做。
- **Decision:**
  1. `board.next_action` 原来只看 stage/project/opening，完全不读三点评分——提纲一写完就说「提纲好了，可以录」，哪怕评分是「素材太薄」。现在没有关联项目时优先看 qa.verdict：thin 说「先补一处再录」，patch 说「先补再录」；已关联项目后不再拦，因为那时人已经决定要拍了。
  2. Anna 的系统提示新增一条硬规则：三点评分里「不要讲过头」标出的细节，回答里不能讲成确定事实或直接引语，要保留原来的不确定语气或明说需要核实。起因是真实对话里 Anna 把评分标了「1840 年历史细节未核实」的例子，直接建议当开场白台词讲。
  3. 每周复盘卡片加一行「数字算于 X 月 X 日」，因为复盘里的倍数是生成时的快照，之后视频还在涨赞，和当天图表的倍数会对不上（验证：复盘存的 6.8× 对应当时点赞 1918，现在点赞已到 2046，图表算出来是 6.7×，两个数字都对，只是时间点不同）。
- **Why:** 这三条都是「同一条信息在两个地方给出不同判断」的同一类问题，会直接让 Park 分不清该信谁。其余三点保留给他定夺，理由见下。
- **Alternatives rejected（本轮不做）：**
  - 对标雷达页的 2.5×/5× 门槛混淆——核实属实（`threshold` 现在被 Park 设成 2.5，`auto_enqueue_threshold` 一直是 5，两个是不同机制，但页面静态文案只写死了「超过 5×」），留给 Park 定要不要在页面上把两个数字都亮出来。
  - 拆解报告里「跑题占比」两处数字不一致——`structure.py` 里代码算的那句是同一个变量两次格式化，理论上不会不一致；如果 Codex 截图里确实看到 2% 和 1%，更可能是模型在某个 segment 的 `reason` 里自己写了一个百分比估算，和代码算的对不上，和第 1 点是同一种「模型自由文本 vs 代码算好的数」漂移，值得以后按同样思路修，但没找到 Codex 指的具体那条报告，不臆造着改。
  - 素材/选题去重、专注阅读视图、报告库筛选——都是真实但更大的改动，且选题 2 和 3 不是简单重复：2 号已经写了文章草稿，3 号有提纲、三点评分和文案，贸然合并会丢工作，留给 Park 决定怎么处理。
- **Evidence:** 真实核对 `threshold=2.5` vs `auto_enqueue_threshold=5.0`（sqlite 直查 + grep 调用点）；真实复盘存储 `6.8×`/likes 1918 vs `/api/mine` 现在 `6.7×`/likes 2046；真实 Anna 对话记录里的原话引用建议；选题 2/3 各自的 drafts 目录内容不同（2 有 article.md，3 有 qa.json/copy.json）。
- **Gotchas:** `next_action` 的 qa 参数是可选的，不传时行为不变，旧调用点不受影响；QA 覆盖只在没有关联项目时生效，一旦开始剪辑就不再用旧的评分拦人。

## 2026-09-16 — Anna 面板滚动：消息和面板共用了 `anna` 这个 class

- **Context:** 修过一次 flex 溢出后，Park 仍看到 Anna 的第一条回复一直停在上面、后面的气泡叠上去。
- **Decision:** 真正的根因是命名冲突：面板用 `.anna` 定样式（`position:sticky; height:100vh`），而 Anna 的每条消息也带 `anna` class，于是每条回复都变成一屏高、贴顶不动的块。消息改用 `from-anna` / `from-park`，面板样式改用 `#anna` 选择器；消息再加 `flex-shrink:0`，长回复不会被压扁。
- **Why:** 上一次只量了面板外框高度，没有量每个气泡，所以没发现。
- **Evidence:** 修复前真实对话 6 条消息里 3 条被压到 138px（内容 595–628px）或撑到 900px 并 sticky；修复后高度 61/150/61/595/102/628，滚动到 11 个位置、以及真实鼠标滚轮滚动，重叠数都是 0，sticky 数为 0。
- **Gotchas:** 以后在 Anna 面板里加新元素，不要再用 `anna` 作 class 名；验证滚动要逐个检查相邻气泡的上下边界，不能只看容器。

## 2026-09-19 — 进项页把财经日报和 K 线日报放回来

- **Context:** 09-16 只留 AI 日报（依据是 18 条视频没有一条来自行情）。09-19 Park 反悔：早上三份日报他都要在进项页看。
- **Decision:** `DAILY_SOURCES` 恢复为 AI 日报、财经日报、K 线日报；晨报是三份的汇总，不再单列。
- **Why:** Park 的话是决定；工作台是他的桌面，他要看什么就摆什么。之前那条推理只解释了推荐材料，不该替他决定桌上放什么。
- **Gotchas:** 推荐 prompt 现在会再次带上行情日报的正文；若发现推荐被行情稀释，可以在 briefing.gather_inputs 里只喂 AI 日报而不动进项页。

## 2026-09-19 — 加工中改成单焦点 pipeline

- **Context:** Park：「任何时候我只能有一条 video in production」「提纲里两条、录制里六七条 doesn't make sense」「要一条 pipeline，milestone 能退回」「首选备选不显眼」「不同意推荐就回更大的池子选」「对标火了的要主动放到首页」。
- **Decision:** 看板不再按阶段分四列。结构变成：今天推荐拍（首选高亮，动作：今天做这条 / 放进选题池 / 暂不拍）→ 正在做的这一条（六个 milestone：选题 → 提纲 → 录制 → 剪辑 → 待发 → 已发出，后三个标「机器」；可退回提纲）→ 对标这周爆了 → 选题池（做这条 / 暂不拍 14 天 / 不做了）+ 剪辑和待发。数据上：topics 加 `is_focus`（全表唯一）、`snoozed_until`、`manual_stage`；阶段仍由文件推导，`manual_stage='outline'` 只在没关联项目时把已有提纲的卡退回提纲，新提纲写完自动清掉。
- **Why:** 焦点是人的注意力，不是文件状态；文件状态继续决定 milestone，焦点决定页面上放大的是哪一条。
- **Alternatives rejected:** 用 status 字段表示焦点（status 已被文章线用作 published，语义混）；把暂缓做成删除（Park 说的是 postpone，不是不做）。
- **Evidence:** 真实数据：8 条在池子里、0 条焦点；点首选「今天做这条」后焦点卡出现，milestone 停在录制，next 显示「素材太薄，先补一处再录」，池子 7 条。
- **Gotchas:** `/api/board` 仍返回 `cards`/`stages`（旧调用和测试用）；焦点卡只认 mine 阶段（提纲/录制），一旦进剪辑就自动从焦点位让出来。

## 2026-09-19 — 触达是第一 KPI；多平台先手填

- **Context:** Park：「我要看的第一个数字是每天触达了多少人」「马上开始做多平台：抖音、视频号、小红书、公众号、小程序、X、B 站、YouTube、小宇宙」「近 30 天发了 6 条改成近 7 天」。
- **Decision:** 已发出页顶上放触达 hero：今天各平台播放合计、近 7 天日均、按这个节奏 30 天、近 14 天柱子。抖音从每日 `video_snapshots` 算「当天涨的播放」；老视频的第一次快照只当基线不计入。其余八个平台没有数据连接，先手填今天的播放，存 `reach_entries(day, platform)`，和抖音加在同一个数里；每个平台可标「开了」和账号名。KPI 条第一格改成近 7 天。
- **Why:** 只有抖音有连接器，但 KPI 不能等八个连接器都做完；手填一格的成本远低于看不到总数。
- **Alternatives rejected:** 只显示抖音（Park 明确说要多平台合计）；把 30 天投影做成曲线拟合（3 天样本，日均 × 30 更诚实）。
- **Evidence:** 修基线前 avg7 = 107,008（把历史播放全算成了 09-14 一天）；修后今天 8,289、日均 2,154。
- **Gotchas:** 每日同步的 launchd plist 之前只写在 `~/.config/content-studio/` 没有装载，所以快照只有手动同步那几天（09-14/15/19）；本次装到 `~/Library/LaunchAgents` 并 bootstrap，每天 9:30 跑一次 `content_studio sync`。没同步的日子触达显示 0，不是没人看。

## 2026-09-19 — 对标雷达默认一周；Anna 改成悬浮窗

- **Context:** Park：雷达默认只看一周，太久的放档案；Anna 别顶得那么紧，做成悬浮的，「需要她的时候出现，不需要就去忙」。
- **Decision:** 雷达加「这一周 / 30 天 / 全部」时间段，默认这一周，计数处写明更早的有几条；页面文案区分「这页显示的门槛」和「自动拆解的 5× 门槛」。Anna 从第三栏改成右下角固定悬浮窗（420×680，手机全屏），不再挤压页面栅格；rail 按钮叫「叫她过来」，窗内按钮叫「去忙吧」。
- **Why:** 一周是他做决定的时间尺度；三栏布局让主内容永远少 380px，而悬浮窗随叫随走，符合「员工」的感觉。
- **Evidence:** 真实数据：这一周 0 条、全部 33 条；Anna 窗 position fixed、距右 22px 距下 22px、页面栅格恢复两栏。
- **Gotchas:** 悬浮窗会盖住右下角内容，输入框和滚动都在窗内；筛选只在前端做，`/api/outliers` 不变。

## 2026-09-19 — 概览按周、数字带说明、推荐进池子、Anna 居中

- **Context:** Park 第二轮反馈：Anna 窗太小太靠角落；有焦点时推荐拍该收起、推荐的两条本来就该在选题池；每个数字要能看到怎么算；14 天柱子看不懂（标签和柱子对不上）；「这 7 天发的」和复盘重复；概览只看一周、我的视频改叫总览并保留作品明细；三页都要有同步按钮；要监控播放/点赞评论收藏/涨粉总粉丝/平均观看/近 7 天条数。
- **Decision:** 推荐生成后自动为每条建选题（按标题去重，含已归档），推荐卡只做高亮，按钮剩「今天做这条 / 暂不拍」；有焦点时推荐折成一行，点开才展开。概览 KPI 条改为一周口径的五格，每格和触达 hero 的三个数各带「?」，悬浮或点击弹出算法说明；14 天图每天一格，没同步的日子画短横线并在说明里写「那天没同步，不是 0」。去掉「这 7 天发的」，复盘占满整行。我的视频改名总览；概览和雷达页头加同步按钮。Anna 窗改为右侧上下各 76px、宽 440。
- **Why:** 「这个数怎么来的」是信任数据的前提；推荐和池子分两处会让人重复建；焦点存在时推荐是干扰。
- **Evidence:** 浏览器：9 个「?」；点击弹出说明；轴标签 09/07…09/19 每两天一个且与柱对齐；12 个「没同步」短线；焦点存在时推荐折成一行、点开后 2 张卡、按钮为「换成做这条 / 暂不拍 / 已在选题池」；Anna 窗 top 76 / bottom 76 / 高 848。
- **Gotchas:** 播放那一格是抖音 7 天涨的播放（和触达同口径），不是 7 天内发出视频的总播放；总粉丝来自账号最近一次同步。

## 2026-09-19 — Anna：一场贯穿所有页面的对话；窗口可拖动、可拉边改大小

- **Context:** Park：在进项和 Anna 聊的，点到加工中或已发出就没了，「这个不该发生」；窗口要能拖、能拉左边框变宽。
- **Decision:** 对话不再按页面分线程，全部存在一条 `main` 线程、一个 Claude Code 会话里。每条消息记下说它时所在的页面（`scope`），页面上显示「在进项」「在《某条视频》」；Anna 回答里的动作按钮按那条消息的页面执行，不按当前页面。换页只换每一轮附带的 <工作台> 资料，系统提示里明确「他换页你不换人」。启动时把旧的按页面分开的对话按时间合并进 main。输入框草稿跨页保留。窗口：拖标题栏移动，四边四角都能拉，最小 320×360，位置和大小存在浏览器里；双击标题栏恢复默认；900px 以下仍是全屏、不能拖。
- **Why:** 一个员工只有一段记忆；按页面切线程等于每换一页就换一个人。
- **Evidence:** 浏览器：进项 / 加工中 / 已发出三页都是同样 12 条消息，草稿跨页保留；左边框从 440 拉到 640 右边不动，拖标题栏移动时大小不变，拉下边框高度 848→648，换页后位置大小不变。真实数据：旧的 `work:3` 和 `input` 两条线合并成一条。
- **Gotchas:** 预览面板只有 673px 宽（手机模式），真实鼠标拖动没法在面板里测，拖动逻辑是用 PointerEvent 在 1440 宽下验证的。「清空」现在清的是全部对话。曾经把 'move' 里的字母 e 当成右边框，拖动时窗口会变窄，已修并有验证。

## 2026-09-19 — 「?」说明图标被拆解队列的 `.q` 样式覆盖

- **Context:** Park 截图：每个数字旁的「?」显示成一个大空椭圆，问号跑到外面。
- **Decision:** 图标 class 从 `.q` 改为 `.help-q`。`.q` 早就是拆解队列每一行的 class（三栏 grid + 内边距），写在样式表更后面，把图标的 15px 圆形覆盖了。
- **Evidence:** 修复后 9 个「?」都是 15×15 圆形，和标签在同一行；拆解队列行仍是 grid 三栏。
- **Gotchas:** 这是本周第二次 class 重名（上次是 Anna 消息的 `anna` 撞了面板）。新加样式前先 `grep -n "\.名字[{ ,.:]" styles.css`，确认没被占用。

## 2026-09-20 — 老师账号：只跟不评分，和对标分开

- **Context:** Park：「有一些博主，虽然他们不是我的对标，但他们说的内容对我非常有用，大部分是教如何做自媒体账号、如何变现的，比如 don't be silent……我应该把他们放在工作台的哪里？」查了一下发现 dontbesilent 本来就在对标库里，56 条作品、12 条爆款，而对标账号的爆款会作为「近两天的爆款」喂进每天的推荐材料、也会顶到看板的「对标这周爆了」——也就是说讲自媒体变现方法论的爆款一直在把选题往非赛道（不是 AI+金融）的方向拽。
- **Decision:** 账号分两类。**对标**回答「拍什么」，爆款照旧喂推荐、喂自动拆解。**老师**回答「怎么拍」：照常同步，但不算中位、不算爆款、不进对标雷达的爆款样本、不进推荐材料、不进自动拆解。老师的新作品按发布时间进「进项」，每条两个按钮：「拆解」学方法，「拿来做」当选题。对标雷达底部加一块「老师」，两边一键互切。dontbesilent 改成老师；Park 给的另外两个博主（小胡也是小狐、一勾工作号）加进老师。
- **Why:** 这两类账号改变的东西不一样——一个改我今天拍什么，一个改我以后怎么判断。混在一起，后者会安静地污染前者。
- **Evidence:** 真实数据：改之前 outliers 里 dontbesilent 占 12 条，改完对标只剩千雪AI 8 条、柱子哥 13 条；三个老师共 152 条作品同步成功，近 7 天 13 条新作品，进项页「老师发的」显示前 6 条；对标雷达「老师 3 个」。旧库迁移：`kind` 列按 is_self 回填成 self / benchmark，线上库验证过。
- **Gotchas:** 进项的老师列表按 `published_at` 取近 7 天，不是按首次抓到的时间——新加一个老师会一次性同步他的全部历史作品，按抓取时间取会把 63 条老视频灌进进项（和之前触达 avg7 把老视频首次快照算成当天涨幅是同一类错）。「拆解结论沉淀进 Anna 的标准」还没做：Anna 的知识文件在只读的 vault 里，要写得先定写到哪。

## 2026-09-20 — 拆解学到的方法写进 Anna 的标准

- **Context:** 老师这条线的最后一步：拆解老师的视频学到的方法，如果只停在「我看了觉得有道理」，下次评分还是老样子。Anna 的知识文件在只读的 vault 里，Park 定：写进 vault 外面的 `~/.claude/skills/park-content-qa/SKILL.md`。
- **Decision:** Anna 多一个动作 `[动作] 记进标准：<一句话>`。按钮上显示完整原句，确认框里再写明写到哪个文件，Park 点了才写。规则落在 SKILL.md 里「## 五、从拆解里学来的」一节，`qa.load_guide` 和 Anna 的 soul 每轮都读，所以一条规则会改变以后每一次评分。设置与 Skills 页能看能删。
- **Why:** 规则文字是从别人视频的转写里提炼的，属于不可信内容——工作台永远不自己写，Park 看着原句点按钮就是那道闸。
- **Evidence:** 线上真文件跑了一整轮：写入后 `qa.load_guide()` 读得到，新一节插在「## 参考与致谢」之前，frontmatter 和权限不变；删掉最后一条后 md5 回到 `70b94c8…`，和写入前字节完全一致。186 个测试通过。
- **Gotchas:** 按字面串 `## 参考与致谢` 切，不按标题走——「## 四、输出格式」的代码块里也有 `## 能不能拍…`，按标题解析会被骗。超过 `qa.MAX_GUIDE_CHARS`（9000）就拒绝写：再往上评分读不到最新几条，而 Anna 的 soul 不截断、还看得到，这种分歧从症状上查不出来。`parse_reply` 的动作正则现在从 `ACTION_KINDS` 拼出来——只往字典里加词而忘了改正则，动作会静悄悄地当普通文字显示。

## 2026-09-20 — Park 的内容原则：principles.md，排在评分标准前面

- **Context:** Park 口述了一批做内容的第一性判断：借力/让别人当主角、微微仰视（干货吸引同行、泛话题吸引客资）、允许被讨厌、不说教、前 1 分钟定生死、抖音是情绪菜市场、可以抄爆款框架，以及他自己的短板「内容有点散，前 30 秒没抓住」。要求放进 Anna 的 skill 里，单独一个 principles.md。
- **Decision:** 新建 `~/.claude/skills/park-content-qa/principles.md`（SKILL.md 旁边，vault 外，Park 自己手改，工作台只读不写）。`qa.load_principles()` 读它，`build_prompt` 把它放进单独的 `<原则>` 块、排在 `<标准>` 之前并写明冲突时以原则为准；`anna.load_soul` 同样排在三点评分之前。优先级做进结构，不只写一句话。
- **Why:** 一句「以本文为准」压不住下面用具体可打分的措辞说反话的标准。所以把 SKILL.md 里真正矛盾的三处直接改掉，让两份文件不再打架：第一句从「必须直接说出主线（话题+钩子+可信度）」改成「一句泛话题的认知冲突」；下结论一节写明优化的是前 1 分钟留存和收藏评论推荐、不是完播率，并把「认知反差 ≤ 2」单列为最严重的一种、平分时选反差高的；「三点在提纲里都有位置」改成「三点各管一段，顺序固定」。outline 和 briefing 的 prompt 一起改，否则 Anna 写出来的提纲会被她自己的评分扣分。
- **Evidence:** 真文件跑通：principles 1792 字被读到，评分 prompt 里原则在标准之前，Anna soul 的来源变成 9 个文件（principles.md 排在 SKILL.md 前）。190 个测试通过，其中三条新测试分别验证原则在前、缺文件不报错也不触发 RUBRIC 兜底、以及 soul 的顺序。
- **Gotchas:** `load_guide` 在读不到文件时会退回 RUBRIC，`load_principles` 不能走同一条路——没有 principles.md 是正常状态，不是故障，所以它返回空串。两者各有各的字数上限（9000 / 4000），这样 `standard.py` 写「记进标准」时的上限检查仍然只针对 SKILL.md，不受原则长度影响。Park 的 vault 知识库里另有一个 `Anna内容原则.md`（目标人群=已经绝望的人；3 秒一个爽点），内容不冲突但重名，以后要合并得他自己动手，vault 是只读的。
- **未定:** 「让别人当主角」+ 访谈是一个内容形态的决定（要不要开始做访谈），不是评分规则，先原样记在原则里，没有塞进三点。

## 2026-09-20 — 前一分钟不是三点的清单；「3 秒一个爽点」弃用；内容原则只留一处

- **Context:** Park 看完清单后的三条裁决。(1)「前一分钟定生死」被我写成了三点各管一段、都压进前一分钟——他纠正：三点是整篇内容的核心，交付不一定放得进第一分钟。(2) 结构只分两段：第一分钟要紧凑，之后他按自己的顺序讲；「每 3 秒一个爽点」删掉。(3) 角色文件里的内容原则 B1–B5 全删。
- **Decision:** principles.md 第六条重写成「三点是整条内容的骨架 + 两段结构」：第一分钟唯一任务是留人（冲突开场→很快落到痛点共鸣，交付不进来），第一分钟之后不再考核节奏。密度那一句删除。SKILL.md 的 §三.4 和 §三.6 同步改，致谢里把行哥的「三种密度」标为弃用。outline 和 briefing 的 prompt 一起改。vault 里 `content_editor Anna.md` 的「固定判断原则·内容」整节删掉，改成指向 principles.md；数据原则 6–10 重编号为 1–5；knowledge 里的 `Anna内容原则.md` 改成迁出说明。
- **Why:** 同一条规则写在两处，改一处就会忘另一处——这次就是角色文件里的「第一句就是主线」和新原则正面冲突，Anna 同时读到两句反话。
- **Evidence:** `anna.load_soul()` 全文检查：「第一句就是主线」「每 3 秒」「3秒一个爽点」都已消失，「第一分钟要紧凑」「已经绝望的人」在。principles 1921 字，190 个测试通过。
- **Gotchas:** 第一版的删除说明把被删的规则原文抄进了括号里，结果它们又回到了 Anna 的系统提示里——删除记录不能复述被删的规则，只写「删了什么类别 + 原文见 git 历史」。B5（不编造、不承诺收益、不给投资建议）从角色文件删掉是安全的：`SKILL.md` §一.3 和 `anna.py` 的 WORKBENCH_RULES 各有一处，不是唯一副本。这是第一次写 Park 的 vault（一向只读），三个文件都先备份到 scratchpad。`content_editor Anna/RUNTIME-SOUL.md` 是一份旧的角色文件副本，没有被 load_soul 读到，但内容已过时。

## 2026-09-20 — 进项改成一排 tab；老师和对标合回一类

- **Context:** Park：「把老师和对标归为一类，we treat them the same way，他们只要一发视频我这边就要更新」；所有来源放在页面上方一个横的 tab 栏；左列表右详情；进项每条只留「入选题池」一个按钮，拆解、拍过了、忽略都去掉。
- **Decision:** tab 栏：全部 / AI 日报 / 财经日报 / K 线日报 / 对标 / 我写的 / 我收藏的 / Clippings。三种来源在前端归一成同一种行（笔记 / 日报 / 对标新作品），点标题右边读原文。`/api/teachers` 换成 `/api/followed/posts`（全部非本人账号近 N 天的新作品），`/api/accounts` 恢复成返回全部非本人账号，`outliers` 和 `hot.py` 同样恢复。新增 `/api/vault/dailies?key=&limit=` 给三个日报 tab 用。对标雷达收回成一块账号卡片，「改成老师/改成对标」按钮和 `PUT /kind` 一起删掉。
- **Why:** 分成两类的唯一理由是不让老师的爆款喂「今天推荐拍」，而推荐本身马上就要删掉——理由没了，分类就该收回去。留下的是真正有用的那一半：进项能看到他们发的新视频。
- **Evidence:** 浏览器实测：8 个 tab；「全部」32 行（13 条笔记 + 19 条对标新作品），不含日报；K 线日报 tab 列出 40 期，点第一期右边渲染出当天正文；控制台无报错。5 个账号的爆款数全部回来了（dontbesilent 12）。190 个测试通过。
- **Gotchas:** 「全部」不含日报是有意的——日报是一份装着很多条的摘要，放进时间流会把别的淹掉。日报文件名有 `2026-09-20-` 和 `26-09-20` 两种写法，`_daily_day` 两种都认。`kind` 列保留但不再有任何分支读它（删列要重建表）。

## 2026-09-20 — 删掉「今天推荐拍」；选题池只能手动进；加工中改左右布局

- **Context:** Park：「今日推荐直接删掉，首选和备选也删掉」「加工中把推荐删掉，progress bar 保留，退回提纲保留」「不要帮我把任何 topic 自动加到选题池」「放回选题池和暂不拍是同一件事，合并成放回选题池」「布局改成左右：选题池在最左边，选出来的在右边」「选题池里要有颜色或 icon 提示到了哪个阶段」「录制就是一个播放的三角」。另外他让我把现有选题池清空。
- **Decision:** 整条每日统筹管线删除：`briefing.py`、`test_briefing.py`、`/api/briefing*`、`_run_briefing`、`_topic_from_recommendation`、9:00 自动 tick、board 里的 `recommend`、store 的 briefing 方法。加工中改成 `.wk-grid` 两列：左边选题池（sticky），右边正在做的这一条 + 剪辑待发 + 对标本周爆。六个里程碑各有图标（选题 ◎ / 提纲 ▤ / 录制 ▶ / 剪辑 ✂ / 待发 ⏳ / 已发出 ✓），选题池每行用同一套图标做阶段 chip，一眼对得上。焦点卡只剩「打开 / 退回提纲 / 放回选题池」，「暂不拍」删掉；选题池行不再能新建，「自己加一条」输入框删掉。现有 10 条选题池全部归档（不是硬删，可恢复）。
- **Why:** 推荐是唯一的自动入池路径，所以删推荐和关自动入池是同一件事，分开做会留下一个没有出口的写入者。briefing 的 `reads` 字段前端从来没用过，删掉推荐之后整条管线每天烧一次 opus 却什么都不显示——不能证明有用就删。
- **Evidence:** 浏览器 1400px：`.wk-grid` 实测 380px + 688px 两列；进度条六个图标都在；焦点卡按钮只剩「打开 / 放回选题池」；推荐区块不存在；选题池 0 条。175 个测试通过（删掉 briefing 相关 15 个）。
- **Gotchas:** `latest_adjustments()` 不能跟着删——写提纲时还要带上每周复盘定下的调整。`briefings` 表保留（删表要重建），只是没有读写它的代码了。已存在的「暂缓」选题仍然列在折叠区里可以恢复，但不能再新建暂缓。发现一个和本次无关的既有问题：`/snapshots/*.png` 有 17 个 404。

## 2026-09-20 — 提纲分成「前一分钟」和「后面讲什么」；评估收进折叠

- **Context:** Park：「提纲要用上 Anna 的原则，第一分钟的反常识暴论、谁能产生共鸣，这两点非常重要」「后边不能太紧凑，第一分钟最紧；前边每 5–10 秒给一个论点，后边每一分钟强调一下主线或给一个爆点」「提纲要区分前一分钟的 hook 和后面的主 content」「写提纲那块有点复杂了，最该改的一处和不要讲过头不要再出现在这里」「三点能不能先隐藏起来，点一下评估才出现——我更希望它是后台的 principles，Don't mess up with my mind」。
- **Decision:** 提纲的结构从一个 `## 提纲` 改成两节：`## 前一分钟`（3–6 条，每条 5–10 秒，第一句是泛话题认知冲突，中间落到痛点具象，不放交付）和 `## 后面讲什么`（3–8 条，结尾给交付）。校验分别数两节的条数，不合成一个总数。principles.md 和 SKILL.md 各加一条两段两速的节奏规则。提纲页的评分默认只剩一行结论（可以拍 / 7 分 / 评于哪天），点「评估」才展开三点；「最该改的一处」和「不要讲过头」从这块移走。
- **Why:** 一段散掉的钩子藏在总条数里看不出来，而「前 30 秒抓不住」正是 Park 自己点名的短板——分开数，钩子超了就直接报错。评分摊在眼前会干扰他写提纲；他要的是 principles 在后台起作用，不是一张记分卡。
- **Evidence:** 浏览器实测：评分默认显示「素材太薄 | 7/15 | 评于 9/20 | 评估 | 重评」，`.qa-grid` 不存在；点「评估」后出现 3 个 `.qa-pt`；「最该改的一处」整页消失，「不要讲过头」只剩提纲正文里那个小节标题，不在评分块里。175 个测试通过。
- **Gotchas:** 改提纲结构会连带三处测试夹具：`outline_fn` 的假提纲、`/api/topics/{id}/outline` 的手写 markdown、以及 `qa_fn` 假证据里的「开头」二字（新提纲里没有「开头」这个词了，evidence 校验会连续失败三次）。「不要讲过头」仍然是提纲自己的一节，只是不再出现在评分面板里——两者同名但不是一回事。

## 2026-09-20 — 拆解报告改左右布局

- **Context:** Park：「拆解报告上面每一个 item 都在上边，感觉有点太乱了。左侧放对标博主的名字、视频 title 和时间；右侧保持现在的布局；按添加时间，最新的排最上边。」
- **Decision:** `#repPick` 从顶部一排胶囊按钮改成左侧一列（sticky，300px），每行三行：谁发的 / 标题（两行截断）/ 拆的日期 · 中位倍数。右边是报告本身。后端本来就按 `generated_at` 倒序返回，不用改。
- **Evidence:** 1400px 实测 `300px 753px` 两列，30 条报告，第一条是「柱子哥TzFilm｜时薪300…｜9/20 · 19.2×」。
- **Gotchas:** 本周第三次 class 重名：`.rep-grid` 早就是报告正文里「主体 + 右栏 340px」的布局类，还带一条 1100px 的 media query。新外层改叫 `.rep-shell`。加新样式前先 `grep -n "\.名字[{ ,.:]" styles.css`——这条规矩已经写进 09-19 的记录，这次又忘了一遍。

## 2026-09-20 — 发布页照着抖音创作者中心做

- **Context:** Park：「发布这里的页面，应该跟抖音或视频号长得一模一样……如果你没有的话，可以看一下抖音的发布页面，先把抖音的发布页面放在这里。」
- **Decision:** 「标题和简介」那块改成抖音发布页的样子：顶部抖音红标 +「发布视频」，左边表单（标题 / 作品描述带 0/1000 计数 / 添加话题），右边一个手机取景框实时预览描述和 #话题，底部红色「发布」+「存草稿」+「复制」。
- **Why:** 抖音没有自动发布通道（`publisher.PUBLISHERS` 只有视频号、B 站、YouTube），所以这一页的真实作用是「在这里写好、到抖音粘贴」。
- **Gotchas:** 凡是带不过去的开关一律不做假控件——「谁可以看」「允许他人保存」「定时发布」都没有放，只在页面说明和确认框里提醒去抖音那边设。红色「发布」按钮点下去先弹确认：「抖音不能自动发，点确定会复制描述、打开抖音发布页」，然后复制 + 开新标签页。一个写着「发布」却不发布的按钮如果不弹这一下，就是骗人。
- **Evidence:** 浏览器：`.cb.dy` 和手机预览都在，字数 0/1000，底部三个按钮「发布 / 存草稿 / 复制」。175 个测试通过。

## 2026-09-20 — 剪辑进度只认「日期_标题」的文件夹

- **Context:** Park 发现「剪辑进度」里列出了一堆不是视频项目的文件夹，问它们是什么、为什么在那儿。查了一下：`aimoney-stage`（1.6G）和 `remotion-stage-v3`（1.7G）是带 node_modules 的 Remotion 工程；`shotcraft-probe`/`chengfeng-probe`/`gsap-skills-probe` 是 clone 下来的仓库；`remotion-tmp` 里只有一个 `pip` 目录、`rtmp` 里只有 `node-compile-cache`，是缓存目录。都是 09-05 到 09-07 之间留下的。
- **Decision:** `list_projects` 只列名字匹配 `^\d{4}-\d{2}-\d{2}_` 的目录——`create_project` 本来就按「日期_标题」命名。线上从 17 条降到 10 条，全是真项目。按名字关联（`PUT /video-project`）不受影响，仍然能指到任意目录。
- **Why:** 一个 1.7G 的 node_modules 出现在「剪辑进度」里、显示成一条等着剪的视频，是纯噪音。
- **Evidence:** 真盘上跑过：10 个全是 `2026-0x-xx_…`；7 个杂目录消失。176 个测试通过。
- **Gotchas:** 测试里那个叫 `old` 的旧版项目夹要改名成 `2026-01-01_old` 才还会被列出来——Park 盘上所有真的旧项目都是日期开头的，所以这个收紧对他没有损失，但换一个人可能有。那 3.4G 缓存和 node_modules 没有删，是他的文件，只是不再显示。

## 2026-09-20 — 两个我自己造的回归：概览打不开、报告列表被旧胶囊样式盖住

- **Context:** Park：「我的概览怎么没了呢？现在什么都不显示了」「拆解报告左边每一个都有一个圆框，但是字都超出了这个圆框，非常丑陋」。
- **Decision / 原因:** (1) PR #90 删每日统筹时，我按「从 `own_recent_videos` 切到 `# -- 触达`」一刀切下去，中间夹着 `_load_report` / `_run_review` / `/api/review` / `/api/review/generate` —— 每周复盘的四段全被一起删了，概览页请求 `/api/review` 拿到 404，整页渲染成「Not Found」。从 5ad32ee 原样恢复。(2) PR #92 把报告选择器换成左侧列表，但旧的 `.rep-picker button{border-radius:999px;padding:3px 11px}` 还在，它的权重（0,1,1）高于我新写的 `.rep-item`（0,1,0），于是每一行被画成药丸、文字溢出。旧规则只服务那排已经不存在的按钮，直接删掉。
- **Evidence:** `/api/review` 回 200；概览页正常显示触达和 KPI；`.rep-item` 的 border-radius 实测 0px、padding 10px 18px。176 个测试通过。
- **Gotchas:** 按行号区间整段删代码，必须先把区间里的每个 `def` 和 `@app.` 列出来看一遍——我当时只盯着 briefing 这个词，夹在中间的 review 就跟着没了，而且 175 个测试全绿（复盘的接口没有测试覆盖）。这是本周第四次 CSS 同名/权重覆盖：换掉一个容器的内容时，要连同它旧的后代选择器一起删，不能只加新的。

## 2026-09-20 — 对标账号一发新视频就下载转文字，落进 002_对标内容

- **Context:** Park：「我点击他们的文章，因为我们现在没有保存在本地，所以我在右侧根本就看不到，也无法选中它」「对标账户更新了，我先做的事儿应该是把视频下载下来，转成文字稿，放到 Obsidian 里」「要有 filter……还是通过内容去 filter 吧，不要通过点赞收藏评论」。他选了「全部自动抓，不预筛」+ 文件夹 `002_对标内容`。
- **Decision:** 拆解管线本来就做下载→转文字→结构拆解，缺的只是最后一步落盘。`TeardownWorker` 加一个 `on_done` 回调，web 层在里面把文字稿写成 `002_对标内容/<日期> <账号> <标题>.md`（frontmatter 带 source/author/published/likes/倍数，正文是主线 + 为什么爆 + 全文）。`vault.INBOX_SOURCES` 新增这个来源，于是进项的「对标」tab 就是普通笔记：右侧能读、能入选题池、能当素材挂到选题上。同步时 `auto_enqueue_new_posts` 把关注账号近 7 天的新作品全部排队（单次上限 20），原来只抓 ≥5× 爆款的 `auto_enqueue_outliers` 保留，负责窗口外的老爆款。
- **Why:** 素材挂在选题上用的是 vault 路径，所以文字稿必须落进 vault 才能被当成素材——只存在报告 JSON 里做不到这件事。
- **筛选:** 只在**转完之后**判断，因为那是内容第一次存在的时刻：标题像预告/开播/抽奖，或时长 < 40 秒，或全文 < 300 字，就不写这篇笔记。不看点赞收藏评论。
- **Evidence:** 回填了 20 篇历史文字稿，进项「对标」tab 21 条，点开右边出全文；`/api/vault/inbox` 里 source=benchmark。178 个测试通过。
- **Gotchas:** 新加一个对标账号会一次性同步他的全部历史作品，所以排队必须按 `published_at` 开窗 + 限量，否则一次排 63 个下载。`render()` 要认报告里 `thesis` 是 `{text, evidence}`、`why_boom` 是它们的列表，直接 `str()` 会把整个字典写进笔记。这是工作台第二处往 vault 写东西（第一处是今天早些时候改 Anna 的角色文件），vault 不再是纯只读了。

## 2026-09-20 — 标题就能判断的不下载；拆完删掉 media/；测试不再能写进真 vault

- **Context:** Park：「每天抓这么多会不会触发 limit？」「就看一下 title，一看就没什么意义就不要下载了……下载了再保存，它也是先保存了嘛，尽量谨慎 about storage」。
- **实测数据:** 下载缓存 30 条视频 = 1.4G，一条约 47M；其中 `media/` 占 69.5M/70M，`transcript.json` + `metadata.json` + `structured_text.md`（重跑分析需要的全部东西）只有 350K。系统盘只剩 11G 可用。按 3 条/天算，不清理的话两个半月填满。
- **Decision:** (1) `auto_enqueue_new_posts` 排队前先看标题，像预告/开播/抽奖/倒计时的直接不排——省一次抓取，也不在盘上留一份。转完之后的内容判断保留，当第二道网。(2) 报告写完就 `prune_media(content_dir)` 删掉 `media/`，其余文件保留，重跑分析不用重新下载。(3) 抓取量：原来只抓 ≥5× 爆款、每天最多 2 条，现在约 3 条/天，仍是串行 1.5 秒间隔，遇验证页即停；真正的风险不在条数而在盘。
- **格式过了一遍:** 删掉这几轮改版遗留的 37 条死 CSS（`.nl-*` newsletter 条、`.tc-*` 老师发的、`.cb-*` 旧文案框、`.qa-fix`、`.src-*`、`.acct.teacher`、`.ms li.auto`、`.legacy-pool-grid`、`.new-card`），共 2.8KB。留着的死规则就是下一个 `.rep-picker button`。
- **Gotchas（要紧）:** 发现测试会写进 Park 真实的 vault——`obsidian_vault` 设置默认是 `~/park-hands`，测试建的 store 用的就是这个默认值，于是 `_save_transcript` 把假报告写成了 `002_对标内容/2026-05-28 对标号 作品5.md`。已删除该文件，并在 web 测试夹具里把 vault 指到 tmp_path，任何测试都不会再碰到真库。以前没暴露是因为工作台一直只读 vault；今天开了写入口，默认值就成了真实风险。

## 2026-09-20 — 进项显示博主名字；过了时效的对标视频不进库

- **Context:** Park：「在对标内容这里，直接显示博主的名字就好了」「今天第一次添加柱子哥，系统把他很多视频都抓进来了，其中一条 vivo X Fold6 是 6 月 27 号发的……内容进来就是为了服务我自己的选题，半年前的内容跟我现在的选题已经没有关系了」。另外他让清掉 1.4G 下载缓存。
- **Decision:** (1) `vault.inbox` 带上 frontmatter 里的 `author`，进项每行显示博主名字，没有作者的笔记仍显示来源文件夹。(2) `transcripts.FRESH_DAYS = 30`：发布超过 30 天的视频不写笔记。拆解报告照常生成、照常能在「拆解报告」里读——只是不进「进项」。(3) 清掉 1.4G 下载缓存（30 个 `media/` 目录），30 份转写稿一份没少，系统盘可用 11G→13G。(4) 按同一条规则清掉回填时写进去的 18 篇过期笔记，剩 2 篇。
- **Why:** 进项这一页是为「今天拍什么」服务的。第一次加一个账号会同步他的全部历史作品，半年前的视频今天才落盘，在按修改时间排的列表里长得和今天发的一样新——但它对今天的选题没有任何用处。自动排队本来就按 `published_at` 开 7 天窗，漏的是手动拆解和我那次回填。
- **Evidence:** 进项「对标」2 条，分别显示「千雪AI」「柱子哥TzFilm」；`/api/reports` 里那些老视频的报告都还在。182 个测试通过。
- **Gotchas:** `is_thin` 里要写成 `is_stale(published_at, days=FRESH_DAYS)` 而不是靠默认参数——默认参数在 def 时绑定，测试就没法调这个窗口。测试夹具里假视频的发布时间是 5 月，加时效闸之后那条端到端测试必须把 `FRESH_DAYS` 调大才跑得通。

## 2026-09-21 — 对标回填跑完；查清 15 条任务的来路；失败任务会留下半截下载

- **最终数字:** 88 个拆解任务，81 成功 / 7 失败；`002_对标内容` 53 篇文字稿（一勾 24、柱子哥 10、dontbesilent 10、千雪 7、小胡 2）。
- **15 条任务的来路（Park 要求查清）:** 只有两个地方会排队——`cli.sync_everything`（每天 9:30 的 launchd）和 `web._sync_and_queue`（`POST /api/sync` 或 `POST /api/accounts/{id}/sync`）。`~/.config/content-studio/logs/daily-sync.log` 只有 09-20 01:30（enqueued 2）和 09-21 01:30（enqueued 4）两条，排除了 CLI。所以是 web 那条路又跑了一次 `auto_enqueue_new_posts`：单次上限 40，第一次同步排的是最新的 40 条，第二次接着排剩下的 15 条——这 15 条的发布时间正好是 08-22→09-12，即 30 天窗口里较旧的那一截，完全吻合。**机制确定，触发它的那次 HTTP 请求查不出来**，因为服务当时既没开 access log 也没配 logging。已打开两者，并让 `auto_enqueue_new_posts` 每次记下窗口/看了多少/排了哪几条。
- **失败任务会留下半截下载:** 跑完后下载目录还有 548M，全是 4 个失败任务的残留，其中一个 499M。两个后果：占盘；以及 `_find_content_dir` 下次找到这个有元数据没视频的目录、跳过下载，转写永远报 `No video file`——那条 `7684559815099567423` 就这样失败了 3 次。现在失败时删掉没有 `transcript.json` 的目录。
- **Gotchas:** 第一版按 `content_item.json` 找目录，漏掉了那个 499M——它连 `content_item.json` 都没写出来，只有 `media/`，谁也找不到。补了一层按视频 id 目录名的兜底。另外 `_report` 测试夹具的 `generated_at` 写死 2026-09-14，而超过 7 天未读的报告读取时自动归档，今天正好第 7 天，测试从今天开始必然失败——已改成相对当前时间。这类"到某天才炸"的夹具值得全局排查一次。

## 2026-09-21 — 写死日期全局排查

- **Context:** 09-21 有个测试无缘无故变红：`_report` 夹具的 `generated_at` 写死在 2026-09-14，而超过 7 天没读的报告在读取时自动归档，今天正好第 7 天。Park：把这类地方全局排查一下。
- **做法:** 两条路并用。(1) 写了个 pytest 插件 `tools/timeshift.py`，把 `content_studio.*` 和测试模块里的 `datetime`/`date` 换成偏移版，`TIMESHIFT_DAYS=365` 跑一遍看什么会炸。(2) 静态扫描所有日期字面量（34 个函数含有），按「这个函数有没有把时间当参数注入」分成 16 安全 / 18 待查，再逐个人工确认。
- **查出的真雷（已修）:** 两处，都是同一个模式——把时间写死在夹具里，代码却用真实的 `now()` 去比。① `_report` 的 `generated_at`（今天已经炸了）；② `create_time: 1780000000`（= 2026-05-28）出现在 test_web、test_accounts、test_worker 三处，随着时间推移这些假视频会掉出 7 天排队窗、30 天时效窗、90 天触达窗。全部改成相对当前时间。
- **确认安全的:** 18 个待查里没有第三处真雷。`test_creator_metrics` / `test_publish` / `test_review` / `test_today` 都有模块级的 `NOW`/`DAY` 常量，但全部通过 `now=lambda: NOW` 或参数传进去，所有数据都相对这个锚点——固定锚点 + 相对数据是稳的，可以一直这么写。其余是「日期只当标签用」（项目文件夹名 `2026-01-01_old`、审批记录）或正则误报。
- **判据（以后照这个写）:** 夹具里的日期只要会跟真实 `now()` 比较，就必须相对当前时间生成；要确定性就把时钟也注入进去（`now=`），不要写死一个日期再让代码去读真实时间。
- **Gotchas:** `tools/timeshift.py` 伪造不了文件 mtime，所以靠 `vault.inbox`（比较文件修改时间）的测试在拨快之后一定失败——那是工具的假阳性。判断真假看失败原因里有没有日期字面量。另外把 `_report` 的 `generated_at` 改成相对时间后，所有报告时间戳变成同一秒，`/api/reports` 的排序变随机，三个测试跟着挂——按 video_id 加了秒级偏移才稳定。

## 2026-09-21 — 全站 design slop 排查：一段旧样式整块残留

- **Context:** Park 截图：Anna 窗的「去忙吧」字特别大；加工中的「暂缓的 1 条」贴在面板最左边，字也大。要求全站过一遍。
- **查法:** 浏览器 1440 宽逐页截图（两种主题 + 390 手机宽），再用脚本量每页所有按钮的字号/高度组合、找零内边距的裸文字按钮、横向溢出、标题字号分布；另外静态比对样式表里同名选择器的多份定义。
- **根因:** 样式表第 568–598 行是「workbench shell / collect」的早期版本整块残留，和文件顶部的新版本同名（`.reader` `.reader-h*` `.linklike` `.acct-switch select` `[hidden]` `.view.wide`）。它排在后面，所以实际生效的是旧的：`.linklike{padding:0;font:inherit;color:var(--ink)}` 压掉了 `.fold`（暂缓那行）的内边距/字号/颜色；侧栏账号下拉用的是浅色表面样式。「去忙吧」另有一条 `#anClose{font-size:18px}`，那是它还是 × 符号时留下的。
- **Decision:** 删掉残留块和其余同名两写的早期副本（共 15 条），`.fold` 提权为 `.linklike.fold` 不再依赖先后，删 18px 那条。扫描其余：按钮字号只有 12 / 12.5 / 13 / 14 四档且各有固定用途，标题 H1 34 / H2 15 一致，无溢出、无其他裸文字按钮。
- **Evidence:** 实测 fold 12.5px、padding 10px 18px、灰色；去忙吧 12px；侧栏下拉深色。184 个测试通过。
- **Gotchas:** 这是本周第五次 CSS 同名覆盖，前四次都当成个案修，这次才发现是整块残留在作怪。样式表今后每次加规则前 `grep -c "^\.名字{"` 确认只有一处；同名两写、后写的赢，是所有这些「莫名其妙变大/贴边」的共同来源。

## 2026-09-21 — 进项的时间窗：主动来源看添加时间，被动来源看发布时间

- **Context:** Park：进项右上角的 7/30/90 天要换成 3/7/30；而且切换时对标的数量不变。他的逻辑：我写的 / 我收藏的 / Clippings 是我主动加进去的，昨天加的四个月前的文章今天也该出现，看的是添加时间；对标是工作台被动替我收的，一个人发了 500 篇、我昨天才加他，不能 500 篇全算今天的，看的是作者的发布时间。一个 proactive，一个 passive。
- **Decision:** `InboxSource` 加 `passive` 标记，只有 `002_对标内容` 是被动。`vault.inbox` 对被动来源用 frontmatter 的 `published` 当事件时间（没有就不列），主动来源仍按文件的创建/修改时间。进项时间按钮改成 3 / 7 / 30 天，默认 7。
- **Why:** 之前对标数量不随窗口变，是因为 53 篇文字稿都是昨天一次回填写进去的，按文件时间它们全是「昨天新来的」。
- **Evidence:** 线上：对标 3 天 9 条 / 7 天 17 / 30 天 55；Clippings 3 天 0 / 7 天 2 / 30 天 14。185 个测试通过。
- **Gotchas:** 两个已有测试的夹具写对标笔记时没带 `published`，改完立刻挂——被动来源没有发布时间就不列，这是有意的，夹具补上即可。写「对标内容」笔记的地方（`transcripts.render`）本来就写 `published`，回填的 53 篇也都有。

## 2026-09-21 — Anna 的上下文从「当前页」扩到「后端目录 + 最近发生了什么」

- **Context:** Park：前端只是后端打包出来的，Anna 不该只看当前页；她要知道「有一条东西刚从进项进了加工中」。但他自己也指出不能每轮把 83 份拆解报告全读一遍——「他肯定要有自己的 routing 逻辑」。他选了先做前两层。
- **数据在哪（查清后告诉他的）:** 一个 sqlite（账号 6 / 视频 268 / 快照 2573 / 选题 15 / 任务 91 / 复盘 / Anna 对话）、一个报告文件夹（83 份 JSON）、Obsidian（日报 + Clippings + 收藏 + 原始输出 + 55 篇对标文字稿）、草稿文件夹（提纲/评分）。
- **Decision:** 第一层「工作台全局」——正在做什么、选题池/剪辑待发/暂缓条数、进项近 7 天各来源未处理数、触达、报告总数+最近 8 个标题、关注账号名单。明确标注「这是目录，不是全文」。第二层新建 `events` 表 + `store.log_event`，在 9 个真实转折点写入（入选题池、开始做、放回、退回提纲、提纲写好/失败、拆解完成、同步完成、进项标记），每轮带最近 12 条。第三层「[查] 某份报告」按需取暂不做。
- **Why:** 目录比全文更常用——她需要知道「有 83 份报告」的频率，远高于需要读其中某一份。每轮增量约一屏，不是几万字。
- **Evidence:** 线上真问一句「现在整体什么情况」，她答「看板空了、4 天没拍，27 条进项堆着没处理，只有一条素材太薄的在等你补」——这四件事分别来自看板、拍摄记录、进项统计、QA 评分，全是页面之外的。186 个测试通过。
- **Gotchas:** `reports = reports()` 会把 `reports` 变成局部名、调用直接抛 UnboundLocalError，而我给整块加了 `except` 兜底，于是报告那行静默消失、测试才发现。给「不能破坏正常流程」的代码加宽 except 时，务必有断言覆盖它的成功路径。`log_event` 自身吞掉 sqlite 错误：记录不该拖垮它描述的动作。

## 2026-09-21 — 左下角换成平台条；各平台手动填写卡；九个平台全开

- **Context:** Park：左下角那个「6 个账号」应该只显示我自己的；九个平台我都有号，要能看到连没连上，亮/暗或灰/彩都行，按你审美；已发出要有各平台的样子；手动上传时把每个空该填什么备好，我复制粘贴。
- **他说的和代码对不上（已在回复里问他）:** 他说公众号/小程序/X/B站/YouTube 能自动发、视频号手动。代码里 `publisher.PUBLISHERS` 只有三个：视频号、B站、YouTube——**视频号恰恰有通道**，而公众号和 X 没有。三个通道的 cookie 都在，但都超过 30 天（05-25/05-29），需要重新登录。
- **Decision:** 左下角换成九个平台字牌（`reach.PLATFORM_STYLE` 给每家一个汉字/字母 + 品牌色）。**三种状态不是两种**：已连接=彩色、要重新登录=彩色带感叹号、手动=灰描边。一个永远亮不起来的灯就是骗人，所以「没有通道」不能显示成「没连上」。新增 `/api/platforms`，连接状态复用已有的 `publisher.readiness()`（它已经算了 `likely_expired`，不另写一份）。发布页每个开了的平台一张卡：标题/正文/话题按那家的字数上限裁好，每个空一个「复制」，带「去 X 上传 ↗」。`copypack.PLATFORMS` 补上公众号/小程序/小宇宙三家规格。
- **不放真 logo:** 那是别家的商标，不从网上扒图打包进来；字牌 + 品牌色同样认得出。
- **Evidence:** 九个字牌全渲染，状态 3 stale / 6 manual 与凭据实际情况一致；发布页 9 张卡 26 个复制按钮，小红书标题自动裁到 20 字并标「已裁短」，X 按 280 字、话题取前 3 个。186 个测试通过。
- **Gotchas:** 左下角原来的「当前账号」下拉被平台条替换，它的 `onchange` 绑定要一并删掉，否则 `$('#acctSwitch')` 返回 null 直接报错。`.plats`（加对标弹窗里的平台胶囊）和新的 `.plat` 是两个类，不冲突——但这类只差一个字母的类名以后要避开。

## 2026-09-21 — 平台状态第四种：平台限制 ≠ 登录过期；公众号实测没被封

- **Context:** Park：视频号腾讯最近两个月开始封自动发布，以前能用；公众号你也试一下，可能也被封了；X 的自动通道你来做。
- **实测公众号:** `~/.config/park/secrets.yaml` 里有 `wechat.appid/secret`。用官方 `cgi-bin/token` 接口试了一次（只读，不发任何东西），返回 **40164 invalid ip**——**凭据有效、公众号没被封**，卡在 IP 白名单：这台机器的出口 IP 不在名单里（`curl api.ipify.org` 能查到本机的，微信的报错里也会带）。加进公众号后台的白名单就能用。家宽 IP 会变，变了要重加。
- **Decision:** 状态从三种加到五种。`publisher.readiness` 认 `blocked` 字段：视频号标记为平台限制，`login_hint` 清空、按钮由状态挡住，**但通道定义留着**（万一他记错或以后解封，不至于把能用的路径删掉）。公众号和 X 标 `setup`（差一步配置），各自说清差哪一步。字牌：已连接彩色、要重新登录彩色带感叹号、差一步配置半透明带感叹号、平台限制虚线灰、手动实线灰。
- **Why:** 视频号原来显示「要重新登录」——重新扫码治不好平台侧的封禁，这条提示会把 Park 支去做无用功。和「永远亮不起来的灯」是同一个原则：状态必须说清真正卡在哪。
- **X 通道没做:** `secrets.yaml` 里没有 X 密钥，要 Park 先去 developer.x.com 建应用。先写脚本等于照猜的 auth 形状写一份测不了的代码。已在回复里向他要凭据。
- **Gotchas:** 「试一下公众号」我只做了认证测试（只读），没有往他账号推草稿——那是对外动作，要他明确点头才做。

## 2026-09-21 — X 纯文字通道（等 Park 填密钥就能用）

- **Context:** Park：X 的自动通道需要你做；问他发文字还是视频，答「主要发文字」。
- **Decision:** 新建 `content_studio/x_post.py`，不走 content-ops：X 是纯 HTTP 调用，不需要那边的浏览器基础设施，放在本仓测试也方便。OAuth 1.0a User Context 用标准库签（hmac+base64，约 30 行），不为此引入 `requests_oauthlib`——这个服务在 launchd 下跑，少一个依赖少一处故障。发的是 `POST /2/tweets`，纯文字；视频要走分块上传和付费层，他说不需要。
- **两个结构性调整:** ①`readiness` 认 `needs_keys`：X 的凭据不是一个文件而是 `secrets.yaml` 里的一段（公众号密钥也在同一个文件里，所以「文件存在」证明不了什么），要逐个 key 查，缺哪个报哪个。②`build_payload` 认 `no_video`：X 发的是正文本身，没有「标题」也没有成片，原来那两个必填会把他卡在根本不存在的字段上。发布面板同样放行。
- **Evidence:** 签名算法用手算的 HMAC 对比验证过；401/429 各有能行动的中文提示；193 个测试通过。线上 X 显示「还缺 api_key, api_secret, access_token, access_secret」，发布面板显示「还没有成片，但 X 发的是文字，现在就能发」。
- **没做的:** 没有真发过任何一条推——没有凭据，也没有他的明确许可。等他把四个密钥给我。

## 2026-09-21 — X 密钥打通（卡在余额）；公众号凭据修好，状态改成真实检测

- **X:** 在 Chrome 里替 Park 建了 App、把权限改成 Read and write、生成 Access Token；密钥全程不经过对话——写了 `~/bin/park-x-keys`（getpass 不回显、不进 history、直接写 secrets.yaml 权限 600）。四个密钥有效，只读接口认出了他的账号。**但发推返回 402 credits depleted**：X 现在是预付费，账户余额 $0，购买档位 $5/$25/$100/$250/$500。没有替他充钱，也没有发出任何一条推。
- **排查过程里最有用的一步:** 401 时先用 X 官方文档的测试向量验证签名算法（`hCtSmYh+iHYCEqBWrE7C7hYmtUk=`，一字不差），**确定性地把问题从算法切到密钥**，否则会在自己的代码里空转。然后靠长度特征定位到 api_key 错了（19 vs 25）——X 页面上那个复制按钮给的是残缺值，点「Show」手动读才是完整的。
- **公众号:** Park 重置 AppSecret 后通了。新建 `content_studio/wechat.py`：真去问微信，**带 6 小时缓存**（取令牌每天有次数上限，每次渲染页面都问会耗光配额）。错误码分开翻译——40164=IP 白名单、40125=密钥不对、45009=配额用完，三件完全不同的事，混成「被封了」会去修错的东西。
- **状态第五种 `ready`:** 凭据有效但发布通道还没做。公众号现在是这个状态——图文发布要先上传封面图再调 draft/add，而 Park 的内容主要是视频，值不值得做要先问他。
- **Gotchas:** 浏览器窗口被缩小时坐标点击全部失准（第一次保存权限就是这么失败的，页面看着没变其实没存进去）。之后全部改用 `find` + ref 点击，不依赖坐标。

## 2026-09-21 — 通道状态改成真探测；X 首推发出；YouTube 重新授权

- **X:** Park 充值后，发出第一条测试推（内容「测试」，他点头才发的）。通道全链路打通。
- **YouTube:** `youtube_channel.py check` 报 `invalid_grant`，确实过期了。跑 `auth` 重新授权，token_saved → check 返回 `token_valid`。
- **B 站其实一直是好的:** `sau_cli.py bilibili check --account creator` 返回 `valid`——cookie 文件四个月没动，按 mtime 判断「早该过期」，实际能用。**差点让 Park 白做一次扫码登录。**
- **Decision:** 新建 `channel_probe.py`。通道 spec 可以带 `probe`（一条命令）和 `probe_ok`（输出里该出现的字样），`readiness` 有探测就以探测为准、没有才退回看文件日期。结果缓存 6 小时（探测要开浏览器或走网络）。探测本身失败时**保留上一次结论并标 `stale_probe`**，不把「我问不到」误报成「它坏了」。
- **Why:** 这是今天第三次同一个毛病——用间接证据猜状态：视频号猜成「登录过期」（其实是平台封禁）、公众号猜成「被封」（其实是 IP 白名单/密钥）、B 站猜成「过期」（其实好的）。能直接问就别猜。
- **Evidence:** 九个平台状态全部核对：X / B 站 / YouTube 已连接，公众号凭据就绪，视频号平台限制，其余手动。197 个测试通过。
- **未决:** 公众号发图文要先传封面图再调 draft/add，Park 说「公众号发文字」——具体是视频的文字稿还是研习室长文，还没问清就不动手。

## 2026-09-21 — 公众号选 A：工作台写文章 → 交接给 Park 原有管线

- **关键发现（差点白做一遍）:** Park 早就有完整的公众号管线。`004_内容加工中/<主题>/wechat-package/` 里有橄榄手记排版模板、`cover/` 封面渲染器、图文 QA、API 草稿回读验证、带 media_id 的回执；发完在 `005_自媒体发出内容/` 留总指针。7 月 14 日那篇就是这么发的。我本来准备从零写 `draft/add`，翻到这个才停手。
- **Decision（Park 选 A）:** 工作台的活儿到「文章按管线的入口格式落进 vault」为止。新建 `handoff.py`：只写两个文件——`README.md` 和 `wechat-package/wechat-article.md`（frontmatter 带 title/author/summary/from_workbench）。**不伪造**下游自己会产出的东西：配图规划、QA、回执、封面。`coverImage` 留空并注明「待 cover/ 渲染器生成」。已存在的主题目录拒绝覆盖。
- **Why:** 那套管线是照着公众号的实际排版效果打磨过的；`draft/add` 还强制要 `thumb_media_id`，而封面正是 `cover/` 渲染器的产物——从零写第一步就卡死。工作台的价值在「选题→文章」，不在「把文章排成公众号的样子」。
- **顺手修了一个没人会发现的故障:** 密钥存在两处，`build-api-draft-proof.mjs` 读的是 `~/.park-secrets/wechat/mp.env`。Park 今天重置 AppSecret 之后，**那套老管线已经坏了（40125），而且不会报错**——下次他去发才会发现。已验证后同步过去，现在两处都能换到令牌。
- **Evidence:** 真跑了一次交接（topic-10），目录名/frontmatter/正文都符合 7 月那篇的格式；验证完把试跑目录和事件都清掉了。201 个测试通过。
- **Gotchas:** `/api/topics/{id}/handoff` 已经是研习室那条线的路径，我的新端点同名会把它**整个盖掉**（测试里 `handed["topic"]` KeyError 才暴露）。改叫 `wechat-handoff`。FastAPI 里同路径同方法不会报错，只会静默覆盖——加端点前先 grep 路径。
- **边界:** 回执里写的 `credential_source` 指向 vault 的 `_secrets/`，那是禁止读取的目录，没有跟进去；`~/.park-secrets/` 是家目录下的另一个路径，不在禁区。

## 2026-09-21 — Park 自己写的东西没有时效性

- **Context:** Park：「我写的这些内容应该是没有时效性的呀，只要是我写的、没拍的，都可以放在这里。」他举的例子：`003_park原始输出/商业模式/新平台.md` 是 7 月 19 日写的，30 天窗口把它挡在外面，于是在工作台里根本看不到——但它随时都能拍。他同时要求这一栏改名叫「Park 原始输出」，和文件夹一对一。
- **Decision:** `InboxSource` 加 `timeless` 标记，只有 `003_park原始输出` 是。它跳过时间窗直接进列表；「还没处理过」交给 triage / used_by 判断，不靠时间。这一栏改名「Park 原始输出」。
- **Why:** 三种来源现在有三种时间语义，而且各自都对：**我主动加的**（Clippings / 收藏）看我什么时候加；**工作台被动收的**（对标）看作者什么时候发；**我自己写的**不看时间——它是素材库，不是新闻流，写过还没拍的就该一直等在那儿。
- **不进「全部」:** 67 条一次性涌进「全部」会把当天真正新进来的东西压到看不见。所以它只在自己那一页整片出现，「全部」保持「这段时间新来的」这个语义。同样的理由，日报也不进「全部」。
- **Evidence:** 线上：「Park 原始输出」67 条不随窗口变，最老的是 7 月 19 日那篇「新平台」；「全部」仍是 26 条。那一页顶部换了说明文案，不再讲时间窗。202 个测试通过。
- **Gotchas:** 抽 `_item()` helper 的时候先写了调用、后补的定义——中间跑一次会 NameError。改结构时先确认函数存在，别依赖「等会儿补」。

## 2026-09-21 — 我自己的内容也要有文字稿库（005_我发出的视频）

- **Context:** Park 想让我判断「我的内容适不适合走 egoego 那条路线」。一查才发现：**对标账号有 55 篇完整文字稿，他自己的一条都没有**。Obsidian 里 `005_自媒体发出内容/` 那 3 篇是公众号指针，跟抖音视频无关；数据库里 19 条只有标题和点赞；只有 4 条拆过。给别人建了资料库，没给他自己建。
- **Decision:** 新建 `005_我发出的视频/`，和 `005_自媒体发出内容/` 平级——后者是公众号图文的指针，前者是抖音视频的文字稿，两种东西不混。`_save_transcript` 按 `is_self` 分流。
- **自己的内容不做筛选:** 对标那套「标题像预告就丢、太短就丢、超过 30 天就丢」**不适用**。分析自己的风格时，短的、没爆的、失败的那几条同样是证据。只有对标才需要过滤，因为那是别人的流。
- **图文帖也要有:** 19 条里 3 条是图文，没有口播可转写。照样各写一篇，`format: 图文`，正文放发布时的文案全文并注明没有口播——「哪几条是图文」本身就是分析材料，跳过它等于丢掉一个信号。
- **Evidence:** 3 条图文已落盘；12 条视频排进拆解队列（4 条之前拆过，不重复）。202 个测试通过。
- **Gotchas:** `write_note` 原来把文件夹写死成常量，加第二个目标时要先把它变成参数——否则两边会写进同一个文件夹，而且要等到 Park 打开 Obsidian 才发现。

## 2026-09-21 — profile.yaml：把「一个人要告诉工作台的所有事」收进一个文件

- **Context:** Park 想把工作台变成别人也能装的产品，提出先做一个标准化配置文件（社交平台链接、后台 AI、对标账号、Obsidian 路径），放进仓库，改 README，并在别人 fork 安装时提示必填/可选。他明说：先建标准，后续慢慢调。
- **先数了数要脱钩的东西:** 14 处写死的本机路径、124 处写死的「Park」、9 个假设的 Obsidian 目录名、5 个必须另装的外部程序。这一步只搭骨架，不把 124 处一次换完。
- **Decision:** `profile.example.yaml` 进仓库当模板，每项标 [必填]/[可选]；`profile.yaml` 进 .gitignore。`profile.py` 只做三件事：找文件（环境变量 → 仓库根 → `~/.config/content-studio/`）、读、逐项核对。**必填项校验的是「填对没有」不是「填了没有」**——抖音链接必须是主页不是视频，`ai.backend` 只认 `claude-cli`（API key 方式没接入，写了直接拒，不假装支持），文件夹必须真的存在。`python3 -m content_studio check` 打清单，必填齐了退出码 0。`serve` 缺项照样起，但终端打一遍清单、页面顶部挂横幅列出缺什么。
- **第一处真正由配置驱动的行为:** `vault.configure()` 用 profile 的 `folders` / `dailies` 重建 `INBOX_SOURCES` / `DAILY_SOURCES`。可选来源没配就不列——列一个不存在的文件夹只会在进项多一个永远空的 tab。没给的保留默认，所以 Park 不配也和以前一样。
- **profile 只填空不覆盖:** 启动时把 vault 路径、项目目录、平台账号种进 settings，但设置页手改过的值优先；自己的账号和对标账号只登记不同步——第一次启动就去抓六个账号会把人吓跑。
- **Evidence:** 用 Park 的真实值生成了一份 profile.yaml（6 个对标、8 个平台、3 份日报），`check` 全 ✅，重启后进项四个来源计数和日报列表与之前一模一样。210 → 212 个测试通过。
- **Gotchas:** `argparse` 的 subparsers 变量叫 `commands` 不叫 `sub`，抄模板时写错一处，import 时不报、跑到那行才 NameError——新增 CLI 子命令后要真跑一次。`create_app` 的 `profile=None` 是默认值，所有旧测试路径不受影响；但 `/api/state.setup.present` 在没 profile 时是 False 而不是缺键，前端横幅靠它区分「还没配」和「配了但缺项」，措辞不同。
- **Gotchas（第二个）:** `vault.configure()` 第一版从「当前」`INBOX_SOURCES` 重建——跑过一次配置，被去掉的可选来源就再也回不来，`_allowed_folders()` 的白名单跟着缩，后面三个毫不相干的测试挂在「找不到素材」「同步没完成」上。改成从冻结的 `_DEFAULT_*` 重建、`configure(None)` 复位，再加一个 autouse fixture 每个测试后复位。**凡是让配置改模块全局的地方，都得同时给出「复位」和「每次从默认重建」，否则热加载和测试都会串。**
- **没做（下一层）:** 124 处「Park」、提示词里的作者名、Anna 的角色文件路径、外部仓库路径——这些还是写死的。profile 里已经留了 `me.name` / `ai.command` / `ai.*_model` 的位，下一步一处一处接。

## 2026-09-21 — 发布台：一条内容铺在所有平台上

- **Context:** Park 的原话是「把每个平台的上传页截个图铺开，没发的是灰的，发了的变彩色」。他每个平台都有号，但发一条内容要在 8 个后台之间来回切，发没发过只能靠记。
- **Decision:** `/api/publish/desk` 把「一条内容 × 每个平台」算成一张表。**四种对待方式（treatment）由通道本身决定，不靠猜**：`manual` 没通道要自己发；`scan` 有通道但靠扫码 cookie；`auto` 密钥在文件里直接发；`handoff` 工作台只写到正文、交给既有管线。
- **为什么 treatment 不写死在前端:** 平台的状态会变（视频号这两个月刚被限制自动发布），写死就要改两处。通道有没有、登录过没过期，都从 `publisher.readiness()` 的实时探测来。
- **Evidence:** `_platform_rows()` 从 `/api/platforms` 抽出来复用，`readiness()` 一次请求只算一遍。219 个测试通过。

## 2026-09-21 — 写大纲改成双轨：保真版 / 重构版两个按钮

- **Context:** Codex 按 Anna 的角色写了两份编辑框架（保真版不重排、重构版允许重排），Park 确认它跟「写拍摄提纲」是同一个功能。他的要求：一个按钮变两个，`don't be silent + ego 保真版` / `don't be silent + ego 重构版`，**两版都留着**——「我要在用起来之后，才知道哪一个版本是我可以接受的」。重构版是 default。
- **提示词搬出代码，搬进 Obsidian:** 原来 `outline.py` 里写死了 80 行中文提示词。现在只留调度，规则从 `001_role/content_editor Anna/workflows/*.md` 读。**框架文件找不到就当场报错，不偷偷退回旧提示词**——否则 Park 改了框架、以为生效了，其实没有。`start_outline` 在起线程前先读一次，让错误在点击的那一刻出现，而不是等两分钟。
- **Park 说「把规则拿掉」:** 两份框架原来 236 / 330 行，砍到 69 / 77 行。判据是**这条规则会不会改变模型的输出**：留下的是它默认会做错的（编造数字和客户结果、第一分钟摆业绩、把不确定写成事实、讲课口吻、保真版搬家、重构版塞三条主线）；删掉的是 16 条验收清单、内部语义地图、内容原子表、以及一堆「只有……才能……」的限定——模型读了不会更小心，只会把真正重要的几条稀释掉。
- **结构校验从代码里撤掉:** 原来 `extract_outline` 检查 `## 前一分钟` 3–6 条、`## 后面讲什么` 3–8 条。两版的形状本来就不一样，再在代码里查结构等于把框架抄第二遍，改框架时一定对不上。现在只校验下游真读的三件事：有标题、有 `## 主线`、主线之后有正文——`opening.thesis_for()` 和三点评分都从 `## 主线` 解析。
- **两版不互相覆盖:** 文件名带 mode（`outline-restructured.md` / `outline-faithful.md`），`outline_path` 指向最近写的那版，新增 `outline_mode` 列。两版都在时提纲页才出现切换；只有一版时不画——多一个按钮只会让人以为另一版坏了。老选题的 `outline.md` 没有 mode 字段，照样读得出来。
- **Evidence:** 222 个测试通过（新增 6 个，覆盖缺框架文件、空框架、两版并存、老文件兼容）。测试用 `CONTENT_STUDIO_WORKFLOWS` 指向 tmp 副本，不读 Park 真实 vault——他随时会改框架，读真文件测试就开始飘。
- **Gotchas:** 「主线之后有没有正文」第一版用 `re.sub` 把主线那一节切掉再数字数，正则的 `\Z` 分支在没有第二个 `##` 时会把整篇正文一起吃掉，合格的稿子被判「只有主线没有正文」，重试两次后失败。改成从主线那一段的结束位置往后数。

## 2026-09-21 — 发布台 · 补记（前端验证与两处坑）

- **Context:** 发布台的主体在 #119 / #121 里（另一个会话从同一份工作区打包发出）。这里只记那两条 PR 没写到的。
- **Park 要的那个体感做到了:** 本地 8781 上九张瓦片 5+4 排开，全灰；点抖音「标为已发」后那张变彩、盖 ✓ 已发 · 9/21 章、计数 1/9；浅色 / 深色都看过。瓦片是 720×480 的画布等比缩小，弹窗里放大，同一份 HTML 两个尺寸。
- **Gotchas:** 页面每 15 秒重取一次数据，第一版的重画签名用的是取数时间，等于每次都重画——弹窗里正在输的链接会被抹掉。改成按内容签名（哪个平台发了没、任务状态、文案）才重画，重画时保留输入框的值和焦点（已在 #121）。**凡是有输入框的面板，重画条件不能是「取过数」，得是「数变了」。** 另：`<dialog>` 里的元素引用每次重画后都失效，浏览器自动化要按坐标点。
- **Gotchas（第二个）:** 线上 8780 有 25 个拆解任务在跑，开发预览不能再起一个带 worker 的实例读同一个库——两个 worker 会抢同一批任务重复下载。预览用 `create_app(start_worker=False)` + 库的拷贝跑在 8781。
- **Gotchas（第三个）:** 两个会话共用一个 checkout，一个在写、另一个把工作区打包发 PR，中间分支还被切走了。同一台机器上并行的会话要么各用各的 worktree，要么先约好谁负责提交。

## 2026-09-21 — 爆款从看板搬进进项，并且排到前面

- **Context:** Park 在看板上看到「对标这周爆了」那一块，说它放错地方了：「它应该放在『进项』那里……这相当于告诉我 OK Priority，应该优先看这些东西」。不要新开一个 section，就混在进项里，但要看得出来。
- **Decision:** 删掉看板的 `attentionBlock` 和后端的 `attention_breakouts()`，改成给进项每一条打标记。对标文字稿的 frontmatter 里有 `source: …/douyin.com/video/<id>`，所以笔记和视频记录能对上——`hot.mark_breakouts()` 做这个 join。
- **倍数和中位数一起给:** 5.3× 单独拿出来没有意义，得知道这个作者平时多少。右上角是倍数，下面一行写「平时中位 1200 赞，这条 6360」。
- **不新造一个黄:** Park 说换成黄色。`--hot`（浅色 #B7770A / 深色 #F0B540）本来就是全站的爆款色——图表上的爆款点、倍数 pill 都是它。再调一个黄会变成两套语义。
- **排序在前端做:** 后端仍然按时间返回，`hotFirst()` 用稳定排序把爆的提到前面、其余保持时间顺序。**时间窗仍然只由 3/7/30 决定**——排序只改窗口内的先后，不会把窗口外的东西捞进来。
- **Evidence:** 225 个测试通过（新增 3 个：video_id 解析、只标爆的、不丢条目不乱序）。
- **Gotchas:** `.in-row.blew` 和 `.in-row.on`（选中）特异性相同，写在后面就会盖掉选中态，点开哪条都看不出来。背景那条写成 `.in-row.blew:not(.on)`。另外 `noteRow` 一直没把 `i.url` 映射出来，所以那颗「去抖音 ↗」按钮从来没出现过——这次一起补上。

## 2026-09-21 — 双轨框架收敛成「一勾式开头和结尾」

- **Context:** Park 看完第一份重构版稿子，判定「写得不好」。查下来最实的一条：稿子圈的人不是他的观众。但他随后澄清那是**故意换的**——这条视频的目的就是让做自媒体的人看到，结尾承接「有私域流量不知道怎么转化的来找我」。于是他重新定义了这个功能：**「不用想 don't be silent 了，我自己的内容就是 don't be silent 的内容，我的思考足够深度」**，只要开头和结尾，不要大纲，两头都用一勾的方式。
- **Decision:** 保真版 + 重构版 → 一个按钮。保真版的活（不动原稿、只在前后加东西）正好被这个吃掉；重构版是 Park 明说不要的。输出只有三节：`## 主线` / `## 开头` / `## 结尾`，中间空着，那是他自己的思考。
- **规则来自真逐字稿，不是套路总结:** 一勾的 24 篇文字稿已经在 `002_对标内容` 里，直接扒了最近 5 条的开头和结尾，连点赞倍数一起看。两条硬规律：
  - **开头前三句必须有真实数字。** 6.2× 那条砸在第 2 句（100 万粉、营收大几百万）；0.3× 那条前面完全没有数字、资历埋在第二段。
  - **结尾卖得越硬，数据越差。** 6.2× 结尾没有任何 CTA，只回到口诀 + 「你看我这个小号不就是吗」；0.5× 结尾是「评论区预约闭门直播，仅此一场错过就没有了」。所以框架里明确禁止稀缺性和催单，最硬只能到一句邀请——这跟 Park 自己给的拆解「她做得不好的视频：销售过重」是同一件事。
  - **交付可行性 = 把自己当活证据**，不是承诺结果。她把自己的烂数据摊开给人看；Park 的等价物是他真做出来的产品。
- **没有加「观众是谁」这个配置层:** 想过，但 Park 两次说过别管他（「你不用管我」），而且观众换成自媒体人是他这一条的临时决定，不是账号定位。这种一次性的东西写进选题备注就够了，不该变成新的配置。
- **数字不硬编:** 他还没定这条要砸哪个（62 分钟那条 6000 赞，还是产品本身）。框架只写「砸一个来自素材或备注的真实数字，位置在前三句」，具体哪个由备注给。
- **Evidence:** 225 个测试通过。`extract_outline` 只校验下游真读的三节 + 开头至少 120 字（低于这个数说明模型给的是要点不是能念的稿子）。老选题的 `outline.md` / `outline-restructured.md` 照样打得开。
- **Gotchas:** 第一版 fixture 的开头只有 113 字，卡在 120 的下限上连跪两次。**一分钟中文口播大约 250–350 字**——测试 fixture 要用真实长度，示意性的三五句话会把真实阈值测成 bug。

## 2026-09-22 — 标了「不做了」的选题，从进项捡不回来

- **Context:** Park 把《新平台》标成「不做了」，之后想从进项重新加回来。进项显示「已入选题池」，但加工中看不到它，点也点不进去。他的原话：「我发现镜像里我写的那些内容好像都没有了，有的可以加回来，有的加不过来，我不知道为什么。」
- **根因:** 三个地方各对一半，合起来是条死路。
  1. `put_triage(status="topic")` 用 `topic_for_note()` 找选题，**这个函数带 `include_archived=True`**。找到归档的那条就直接返回，不新建也不解档——**接口返回成功，选题却还在归档里**。
  2. `vault_inbox` 的 `used_by` 跳过归档选题，所以笔记看起来「没人占」；但 `triage` 表里那行 `topic` 还在，前端 `taken = i.triage || …` 于是显示「已入选题池」。
  3. `rowActions` 在 `taken === 'topic'` 但没有 `topicId` 时，回落到一个**没有按钮的灰色 chip**。所以既进不去，也加不回来。
- **Decision:** 三处都按「说实话」改：
  - **「不做了」的时候清掉那几篇笔记的 triage**——不做了就是这几篇重新可选，不该留一个指向归档选题的标记；
  - **「入选题池」碰到归档选题就解档**，而不是默默什么都不做。捡回来的是**原来那条**，提纲、备注、拆解都还在上面，不新建；
  - **`used_by` 带上 `dropped`**，进项显示「标过不做了 + 再捡回来」，而不是假装它还在池子里。
- **为什么不是简单地让 `topic_for_note` 忽略归档:** 那样会新建一条，Park 之前写的提纲和备注就丢了。他要的是把那一条拿回来，不是重新开一条。
- **Evidence:** 226 个测试通过（新增 1 个，把「入池 → 不做了 → 再入池」整条走一遍，并断言拿回来的是同一个 id）。
- **Gotchas:** `topic_for_note()` 带 `include_archived=True` 是有意的（避免重复建），但它的调用方都默认「找到了就是活的」。**任何返回归档记录的查询，调用方都得显式处理归档分支**，否则就会出现这种「接口成功、界面无事发生」。

## 2026-09-22 — 标过「拍过了 / 已忽略」的也要能反悔

- **Context:** 上一条修好「不做了」之后，进项里还剩一种死 chip：Park 把《世界发展的演变》标成「拍过了」，那一行就只剩一个灰色的「已入选题池」，**没有按钮**。他说的「有的可以加回来，有的加不过来」，另一半就是这个。
- **两个错:** 一是**文案本身是错的**——`shot`（拍过了）和 `ignored`（已忽略）都被一律写成「已入选题池」，跟真实状态对不上；二是没有回头路。
- **Decision:** 三种状态都是他自己手点的，所以每一种都得能反悔。现在各显示真实文案（拍过了 / 已忽略 / 已入选题池），后面跟一颗「再捡回来」。唯一没有按钮的情况只剩一种：它真的在加工中或已发出——那颗 chip 本身就是入口。
- **Evidence:** 226 个测试通过（纯前端改动）。线上四条笔记分别落在「在加工中 →」「标过不做了 + 再捡回来」「拍过了 + 再捡回来」「入选题池」四种状态上。

## 2026-09-22 — 发布只留一个地方，而且只列真能发的那条

- **Context:** Park 看到发布页顶部「发哪条」摆着六条候选，全是提纲和录制阶段的。他的原话：「通常我只会有一条视频在这个环节中，而不是截图中的六个。」同时要求把「加工中」里的「发布」页签删掉。
- **只列真能发的:** `ready_to_publish()` 只留 `ready`（成片出来了）和 `shipped`（发过了、要补记别的平台）。还在写提纲、还在录的不该出现在发布页——**列一排选不了的东西，等于让人每次重新判断哪条才是真的能发**。
- **空页面要指路，不能只说「没有」:** `waiting_for()` 返回最接近的那条和它卡在哪，页面写「最近的一条是《X》，还在录制。成片文件出现在视频项目目录里，它就会自己走到待发」，并给一颗直接跳过去的按钮。另外折一个 `<details>`「我已经有成片了，工作台还不知道」，列其余的——**不能因为收窄就把人锁在外面**。
- **删页签之前先把东西搬走:** 那个页签里装着四样：文案编辑（copypack）、一键发布面板（publishnow）、抖音视频关联、点赞变化。一键发布发布台自己有一整套 `data-pj-*` handler，publishnow 只有那个页签在用；**抖音关联是 `published_video_id` 的唯一入口，而 `board.is_shipped()` 读的就是它**——直接删会让 Park 再也没法把一条标成「已发出」。所以先把关联搬进发布台的抖音弹窗（复用 `/api/topics/{id}/publish`，后端一行没改），文案搬进发布台的一个 dialog（复用 `window.renderCopyBox`），再删 `publish.js` 和 `publishnow.js`。
- **Evidence:** 227 个测试通过（新增 1 个：只有成片出来的才上发布页，发不了时 `waiting` 指出最近那条，指名 `?topic_id=` 仍能直接打开）。
- **Gotchas:** `publishdesk.js` 里原来有一句「抖音的数据在『加工中 → 这条视频 → 发布』页签里关联后自动来」——正好指着要删的页签。**删一个页签前，先 grep 有没有别的地方在用文字指路**，否则留下的是一句指向不存在页面的说明。

## 2026-09-22 — 开头结尾换成骨架：暴论候选 + 论点证据 + 一句结尾

- **Context:** Park 读完生成的结尾，判定「其实我觉得这段话里只有最后一句话有用」——就是那句「如果你手里有私域流量但不知道怎么转化，欢迎来找我」。他的理由：有文章的时候他就已经有完整逻辑了，中间和结尾都不需要别人替他想。他重新定义了要什么：**5–10 个反常识暴论候选**（他自己挑一两个）、**论点 + 证据的骨架**、**一句结尾**。
- **暴论的规则从 24 条真逐字稿里扒:** 按点赞倍数排，能用的句式就五种——预告会挨骂 + 绝对判断（6.2×）、身份反差（1.9×）、绝对否定（1.5×）、攻击一个流行说法（1.1×）、绝对否定但没数字（0.3×）。共同点是**绝对词**（一定 / 全部 / 不可能 / 压根不存在 / 最大的骗局）+ **否定一个大家默认是好的东西** + **离钱近**。
- **但不许硬拗:** 一勾自己有一条视频专门骂「靠拉踩同行，或者硬拗一个反常识的观点来博眼球」，那条 0.9×。所以框架要求**每条候选都标出它靠原文哪一句撑着**，撑不住的不写。
- **证据允许「待补」，但必须能照着做:** 这是跟上一版反过来的。以前禁止任何占位符；现在 Park 明说要「用数据、真实案例截图来增加真实感」，所以证据分两种写法——素材里真有的直接写，没有的写成**一行具体的待办**（去截哪张图、去查哪个数）。「需要补充案例」这种没法执行的话不算。**绝不许把没有的写成好像已经有。**
- **Evidence:** 228 个测试通过。校验只管下游真读的四节 + 候选至少 5 条（给一条等于替他做了决定）+ 至少 2 个论点（一个不叫骨架）。以前的 `outline.md` / `bookend.md` 照样打得开。
- **Gotchas:** 「缺少标题」那条测试原来靠删掉第一行来触发，但删完之后正文以 `## 主线` 开头，`startswith("#")` 照样成立，测试没测到东西。改成把 `# 标题` 的井号去掉。**构造负例时要确认它真的走到了你要测的那个分支。**

## 2026-09-22 — 口播 Step 3–4 搬进工作台：转写 + 标 Hook 的表

- **Context:** Park 想在工作台里直接干剪辑那几步，不跳出去。先查了一下他已经有什么——`~/.agents/skills/ask-park-video` 里**那张标 Hook 的表已经存在**（`scripts/build_worktable.py` + `assets/worktable/worktable-template.html`，620 行，含 5 个 Hook 槽、视觉标注、localStorage 草稿、导出 JSON）。工作台这边也已经有后台跑 14 步、审批门、worktable 导入。
- **所以没有重做那张表。** 重做一张只会和后面 14 步的格式对不上——`worktable.json` 是 Step 5 和 Step 11 的输入。真正缺的只有两件：
  1. **没字幕时本机转写**——以前要开终端跑 whisper；
  2. **那张表开在工作台里、标完直接写回项目**——以前是在别处打开 HTML，导出的 JSON 落进下载文件夹，再手动拷回 `analysis/worktable.json`。
- **转写要先问:** `mlx_whisper` + `whisper-large-v3-turbo` 本机已经有了。但它要占住机器两三分钟，所以前端先 `confirm`，Park 点过才跑，跑在后台线程里，页面 15 秒轮一次。
- **「保存到项目」是追加不是改写:** 服务那张 HTML 时在末尾**追加**一段脚本，只依赖模板里的全局 `payload()`。`payload` 不在就直接不挂按钮，退回原来的导出流程——skill 升级改了模板也不会把整张表搞坏。
- **找成片的规则:** 项目根目录和一层子目录里最大的那个视频文件，`part-a-hook` / `final` / `renders` / `tmp` 这些中间产物目录跳过——它们里面全是 mp4，而且往往比粗剪还大。
- **Evidence:** 234 个测试通过（新增 6 个：挑成片时跳过中间产物、约定位置的 SRT 优先、state 告诉前端显示哪颗按钮、缺脚本时报出路径、跑的是 skill 自己的脚本、保存按钮是追加的）。
- **没验到的一段:** Park 还没把成片放进项目目录，所以**真实的转写和建表没有端到端跑过**。跑通那一遍要等他把粗剪放进去。

## 2026-09-22 — 剪映导出：直接拿最新的那条

- **Context:** 上一版假设粗剪已经在项目目录里。Park 指出剪映根本不往那儿导——它导到 `视频/剪映导出`，而且**文件名全是纯日期**（`9月22日.mp4`、`9月17日(2).mp4`）。带字幕导出时它建一个同名文件夹，mp4 和 srt 放里面。
- **先做了个挑选列表，Park 否了:** 「就拿最新的就好了，命名用来校对。」所以不列清单，只显示最新那一条，把**名字 + 时长 + 大小 + 时间**摆出来给他核对一眼，一颗按钮放进项目。名字认不出内容，**时长才是能分辨的那个字段**（他这条 11 分 41 秒）。
- **拷进去而不是引用:** 14 步那套要成片在项目目录里。同一块 APFS 盘上 `cp -c` 是 clone——秒级、不占额外空间；跨卷或非 APFS 自动退回普通拷贝。
- **保留日期后缀:** 存成 `粗剪-9月22日.mp4` 而不是 `粗剪.mp4`。项目里躺一个没有日期的「粗剪」，回头对不上是哪一次导的。
- **路径要校验:** `path` 是前端传回来的，落盘前确认它 `is_relative_to` 剪映导出目录——否则等于开了一个「把任意文件拷进项目」的口子。
- **Evidence:** 236 个测试通过（新增 2 个：最新的那条要认对、带字幕的文件夹形态、拷过去之后 `find_video`/`find_srt` 能接上）。线上真实目录跑过：7 条导出全部认出，时长和大小都对。

## 2026-09-22 — 清掉重复的 adopt 端点（挑选列表那一版没删干净）

- **Context:** 先做了「从剪映导出里挑一条」的列表，Park 否了改成「只拿最新的」。但两版都进了仓库：`/video-project/exports` 和**两个同名的 `/video-project/adopt`**，前端也还在跑挑选列表那一版——页面上摆出 7 条让人挑，正是他刚否掉的东西。
- **为什么没当场发现:** FastAPI 对重复路由**不报错**，静默用后注册的那个。测试全绿，服务正常起，页面也有东西渲染——只是渲染的是错的那一版。这和之前 `/topics/{id}/handoff` 撞车是同一个坑。
- **Decision:** 删掉 `/exports` 端点和多出来的那个 `adopt`，前端换成只显示最新那条，顺手删掉没人用的 `.vp-picks` / `.vp-pick` 样式。
- **Evidence:** 三个路径各剩 1 处（`exports` 0 处、`latest-export` 1 处、`adopt` 1 处），236 个测试通过，CSS 花括号配平。
- **Gotchas（两条）:**
  1. **删多行 CSS 规则不能按前缀删行。** `.vp-pick{` 是多行规则，用 `/^\.vp-pick/d` 只删掉了第一行，第二行成了孤儿片段挂在上一条规则后面——CSS 不报错，只是悄悄改掉了别的选择器。删完要数一遍花括号。
  2. **改完一定要看页面渲染的是哪一版**，不能只看测试绿。这次是靠截图才发现跑的是被否掉的设计。

## 2026-09-22 — 转写出来没标点，那张表变成一整坨

- **Context:** Park 点了转写，跑完了，工作台也说生成好了。但 `transcript.sentences.json` 里**只有 1 个句子**——整篇 11.9 分钟。打开那张表根本没法标 Hook。
- **根因两个，都是我的:**
  1. **whisper 转中文默认不给标点。** 706 条字幕、4989 个字，`。，！？` 一个都没有。而 `build_worktable.py` 的 `split_sentences()` 是靠标点断的，没标点就只能切出一句。修法是给 `initial_prompt` 一段**带标点的**示例句——25 秒样本实测：不带 prompt 0 个标点，带 prompt 3 个。
  2. **字幕写错了位置。** `get_writer(fmt, output_dir)` 返回的 writer，第二个参数是**文件名**不是路径：它做 `Path(output_dir) / output_name`。我传了视频的绝对路径，**绝对路径直接盖掉 output_dir**，字幕落到了项目根目录而不是 `subtitles/source.srt`。改成传裸名 `"source"`，并在返回前确认文件真的在。
- **加了一道闸:** `_guard_sentences()` —— 平均一句超过 25 秒就直接报错，而不是交一张没法用的表出去。「跑成功了但产物没法用」比直接失败难发现得多。
- **Evidence:** 238 个测试通过（新增 2 个：一整坨要被拦下、prompt 自己必须带标点）。
- **Gotchas:** 第三方 writer 的参数叫 `output_name` 而不是 `output_path`，传绝对路径不报错、不警告，只是悄悄写到别处去。**凡是「目录 + 名字」拼路径的 API，传进去的那个名字必须是裸名。**

## 2026-09-22 — 标点改成单独一遍，不塞给 whisper

- **Context:** 上一条给 whisper 加了 `initial_prompt` 让它自己断句。标点确实出来了，但**转写质量塌了**：706 条字幕变成 34 条，还出了乱码（`博主朋友Ｂ不管是卖客Ｂ`）。修一个问题制造了一个更大的。
- **Decision:** 改走 `ask-park-video` 本来就留好的那条路。`build_worktable.py map` 有个 `--text` 参数，文档写着「校对后的纯文本（只改标点和错别字）」——它把**校对稿和 SRT 时间轴当两个来源对齐**，正是为这种情况设计的。所以：转写不加任何 prompt（时间轴准、断句细），SRT 拆成纯文本，本机模型只补标点，再 `map --srt ... --text ...`。
- **为什么这样更安全:** `map` 自带漂移守卫——补标点时顺手改了字，它会拒绝出活。也就是说「只加标点不改字」这条要求**有机器在核**，不是只写在提示词里。
- **守卫的顺序也错了:** 原来是先出 HTML 再验断句，守卫拦下来的时候，一张没法用的表已经在盘上了。改成 map → 验 → 出 HTML。
- **Evidence:** 240 个测试通过（新增 3 个：SRT 拆文本、补标点不许改字、没法用的表不许留在盘上）。
- **Gotchas:** 给转写模型加 prompt 去解决下游的格式问题，是在错误的地方使劲。**转写只负责转对，格式化是下一道工序**——而且这条流水线原本就为这道工序留了入口，我一开始没看见。

## 2026-09-22 — 补 project.json：标完 Hook 之后原来没有路通向动效

- **Context:** Park 在工作台里标完（其实是跳过）Hook、存回项目，然后问：「我怎么开始让它做动效呢？就没有 button 能让我走到下一步吧。」确实没有。
- **查下来不是缺按钮，是缺一个文件。** 「开始跑」那颗按钮一直都在（`workflow_runner`，后台跑 14 步、停在审批门），但 `start_run` 会拒：`这是旧版目录，没有 project.json，不能按 14 步继续`。工作台建项目时只写了 README 和拍摄提纲，没写 `project.json`——于是 `inspect()` 判成 legacy，进度算不出来，run 也起不来。
- **没有自己去生成 visual-plan.json:** 想过在工作台里直接做动效规格，看了合同就放弃了——每个镜头要 gallery 卡片哈希、demo 源码哈希、provenance、cue points、数值语义契约，还有 `workflow_guard check --gate visual-spec` 机器校验。**自己拼一份大概率过不了那道闸，而且是静默过不了。** 正确做法是把门打开，让本来就懂这套合同的 agent 去做。
- **四个 preset 的 id 从 skill 目录里读，不写死。** `caption_style` 和 `caption_layout` 同在 `presets/captions/` 下，靠文件名里有没有 `layout` 区分。
- **已经有 project.json 就不覆盖**——里面可能已经记了审批和证据。
- **Evidence:** 241 个测试通过（新增 1 个：preset 解析、写出来的结构、重复初始化要拒绝、缺 preset 要指名道姓）。
- **没解决的:** 「跳过 Hook」在 14 步合同里**没法表达**。Step 5/7/8/9 的判据全是文件存在性（`part-a-hook/individual/`、`edit.json`、`video.mp4`），不看 `step_status` 里的 `skipped`；只有 Step 11 认 skipped。所以真跳过 Hook 的话，进度会一直停在 Step 5。这是 skill 那边的合同，不在工作台这边改。
